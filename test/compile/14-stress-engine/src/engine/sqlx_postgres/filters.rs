// @generated by prisma-rust-generator — do not edit by hand
#![allow(clippy::upper_case_acronyms, dead_code)]
// engine/sqlx_postgres/filters.rs

// @generated by prisma-rust-generator
#![allow(clippy::too_many_lines)]
#![allow(unused_imports)]

use crate::shared::filters;
use crate::shared::filters::QueryMode;

pub(crate) fn push_uuid_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &filters::UuidFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_uuid_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_uuid_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &filters::UuidNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_uuid_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_string_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &filters::StringFilter,
) -> bool {
    qb.push("(");
    let mut any = false;
    let ci = matches!(f.mode, Some(QueryMode::Insensitive));
    let like_op = if ci { "ILIKE" } else { "LIKE" };

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if ci {
            qb.push(format!("LOWER({col}) = LOWER("));
            qb.push_bind(value.clone());
            qb.push(")");
        } else {
            qb.push(format!("{col} = "));
            qb.push_bind(value.clone());
        }
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(value) = &f.lt {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} < "));
        qb.push_bind(value.clone());
    }
    if let Some(value) = &f.lte {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} <= "));
        qb.push_bind(value.clone());
    }
    if let Some(value) = &f.gt {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} > "));
        qb.push_bind(value.clone());
    }
    if let Some(value) = &f.gte {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} >= "));
        qb.push_bind(value.clone());
    }
    if let Some(value) = &f.contains {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} {like_op} "));
        qb.push_bind(format!("%{value}%"));
    }
    if let Some(value) = &f.starts_with {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} {like_op} "));
        qb.push_bind(format!("{value}%"));
    }
    if let Some(value) = &f.ends_with {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} {like_op} "));
        qb.push_bind(format!("%{value}"));
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_string_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_string_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &filters::StringNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;
    let ci = matches!(f.mode, Some(QueryMode::Insensitive));
    let like_op = if ci { "ILIKE" } else { "LIKE" };

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if ci {
            qb.push(format!("LOWER({col}) = LOWER("));
            qb.push_bind(value.clone());
            qb.push(")");
        } else {
            qb.push(format!("{col} = "));
            qb.push_bind(value.clone());
        }
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(value) = &f.lt {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} < "));
        qb.push_bind(value.clone());
    }
    if let Some(value) = &f.lte {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} <= "));
        qb.push_bind(value.clone());
    }
    if let Some(value) = &f.gt {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} > "));
        qb.push_bind(value.clone());
    }
    if let Some(value) = &f.gte {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} >= "));
        qb.push_bind(value.clone());
    }
    if let Some(value) = &f.contains {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} {like_op} "));
        qb.push_bind(format!("%{value}%"));
    }
    if let Some(value) = &f.starts_with {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} {like_op} "));
        qb.push_bind(format!("{value}%"));
    }
    if let Some(value) = &f.ends_with {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} {like_op} "));
        qb.push_bind(format!("%{value}"));
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_string_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_int_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &filters::IntFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(value) = &f.lt {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} < "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.lte {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} <= "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.gt {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} > "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.gte {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} >= "));
        qb.push_bind(*value);
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_int_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_int_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &filters::IntNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(value) = &f.lt {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} < "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.lte {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} <= "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.gt {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} > "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.gte {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} >= "));
        qb.push_bind(*value);
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_int_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_bigint_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &filters::BigIntFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(value) = &f.lt {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} < "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.lte {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} <= "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.gt {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} > "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.gte {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} >= "));
        qb.push_bind(*value);
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_bigint_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_bigint_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &filters::BigIntNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(value) = &f.lt {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} < "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.lte {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} <= "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.gt {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} > "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.gte {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} >= "));
        qb.push_bind(*value);
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_bigint_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_float_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &filters::FloatFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(value) = &f.lt {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} < "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.lte {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} <= "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.gt {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} > "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.gte {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} >= "));
        qb.push_bind(*value);
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_float_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_float_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &filters::FloatNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(value) = &f.lt {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} < "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.lte {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} <= "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.gt {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} > "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.gte {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} >= "));
        qb.push_bind(*value);
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_float_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_decimal_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &filters::DecimalFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(value) = &f.lt {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} < "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.lte {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} <= "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.gt {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} > "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.gte {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} >= "));
        qb.push_bind(*value);
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_decimal_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_decimal_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &filters::DecimalNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(value) = &f.lt {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} < "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.lte {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} <= "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.gt {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} > "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.gte {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} >= "));
        qb.push_bind(*value);
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_decimal_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_datetime_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &filters::DateTimeFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(value) = &f.lt {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} < "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.lte {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} <= "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.gt {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} > "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.gte {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} >= "));
        qb.push_bind(*value);
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_datetime_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_datetime_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &filters::DateTimeNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(value) = &f.lt {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} < "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.lte {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} <= "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.gt {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} > "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.gte {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} >= "));
        qb.push_bind(*value);
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_datetime_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_bool_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &filters::BoolFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_bool_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_bool_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &filters::BoolNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_bool_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_bytes_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &filters::BytesFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(value.clone());
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_bytes_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_bytes_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &filters::BytesNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(value.clone());
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_bytes_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_json_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &filters::JsonFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(value.clone());
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_json_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_json_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &filters::JsonNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(value.clone());
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_json_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum1_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_01_stress::Enum1Filter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum1_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum1_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_01_stress::Enum1NullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum1_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum2_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_01_stress::Enum2Filter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum2_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum2_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_01_stress::Enum2NullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum2_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum3_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_01_stress::Enum3Filter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum3_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum3_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_01_stress::Enum3NullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum3_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum4_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_02_stress::Enum4Filter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum4_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum4_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_02_stress::Enum4NullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum4_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum5_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_03_stress::Enum5Filter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum5_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum5_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_03_stress::Enum5NullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum5_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum6_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_04_stress::Enum6Filter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum6_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum6_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_04_stress::Enum6NullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum6_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum7_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_04_stress::Enum7Filter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum7_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum7_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_04_stress::Enum7NullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum7_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum8_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_04_stress::Enum8Filter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum8_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum8_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_04_stress::Enum8NullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum8_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum9_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_05_stress::Enum9Filter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum9_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum9_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_05_stress::Enum9NullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum9_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum10_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_05_stress::Enum10Filter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum10_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum10_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_05_stress::Enum10NullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum10_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum11_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_05_stress::Enum11Filter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum11_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum11_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_05_stress::Enum11NullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum11_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum12_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_05_stress::Enum12Filter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum12_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum12_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_05_stress::Enum12NullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum12_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum13_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_05_stress::Enum13Filter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum13_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum13_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_05_stress::Enum13NullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum13_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum14_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_05_stress::Enum14Filter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum14_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum14_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_05_stress::Enum14NullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum14_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum15_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_05_stress::Enum15Filter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum15_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum15_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_05_stress::Enum15NullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum15_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum16_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_05_stress::Enum16Filter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum16_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum16_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_05_stress::Enum16NullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum16_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum17_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_05_stress::Enum17Filter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum17_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum17_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_05_stress::Enum17NullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum17_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum18_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_05_stress::Enum18Filter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum18_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum18_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_05_stress::Enum18NullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum18_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum19_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_07_stress::Enum19Filter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum19_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum19_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_07_stress::Enum19NullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum19_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum20_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_07_stress::Enum20Filter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum20_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum20_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_07_stress::Enum20NullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum20_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum21_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_08_stress::Enum21Filter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum21_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum21_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_08_stress::Enum21NullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum21_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum22_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_08_stress::Enum22Filter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum22_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum22_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_08_stress::Enum22NullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum22_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum23_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_08_stress::Enum23Filter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum23_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum23_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_08_stress::Enum23NullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum23_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum24_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_09_stress::Enum24Filter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum24_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum24_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_09_stress::Enum24NullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum24_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum25_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_10_stress::Enum25Filter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum25_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum25_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_10_stress::Enum25NullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum25_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum26_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_10_stress::Enum26Filter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum26_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum26_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_10_stress::Enum26NullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum26_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum27_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_10_stress::Enum27Filter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum27_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum27_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_10_stress::Enum27NullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum27_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum28_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_10_stress::Enum28Filter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum28_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum28_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_10_stress::Enum28NullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum28_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum29_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_11_stress::Enum29Filter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum29_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum29_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_11_stress::Enum29NullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum29_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum30_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_11_stress::Enum30Filter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum30_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum30_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_11_stress::Enum30NullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum30_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum31_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_13_stress::Enum31Filter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum31_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum31_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_13_stress::Enum31NullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum31_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum32_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_13_stress::Enum32Filter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum32_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum32_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_13_stress::Enum32NullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum32_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum33_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_14_stress::Enum33Filter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum33_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum33_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_14_stress::Enum33NullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum33_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum34_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_14_stress::Enum34Filter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum34_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum34_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_14_stress::Enum34NullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum34_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum35_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_14_stress::Enum35Filter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum35_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum35_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_14_stress::Enum35NullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum35_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum36_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_15_stress::Enum36Filter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum36_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum36_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_15_stress::Enum36NullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum36_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum37_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_16_stress::Enum37Filter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum37_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_enum37_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::m_16_stress::Enum37NullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_enum37_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn sort_order_sql(o: crate::shared::filters::SortOrder) -> &'static str {
    match o {
        crate::shared::filters::SortOrder::Asc => "ASC",
        crate::shared::filters::SortOrder::Desc => "DESC",
        crate::shared::filters::SortOrder::AscNullsFirst => "ASC NULLS FIRST",
        crate::shared::filters::SortOrder::AscNullsLast => "ASC NULLS LAST",
        crate::shared::filters::SortOrder::DescNullsFirst => "DESC NULLS FIRST",
        crate::shared::filters::SortOrder::DescNullsLast => "DESC NULLS LAST",
    }
}

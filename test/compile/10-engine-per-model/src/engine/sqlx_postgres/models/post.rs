// @generated by prisma-rust-generator — do not edit by hand
#![allow(clippy::upper_case_acronyms, dead_code)]

use sqlx::Row as _;

impl sqlx::FromRow<'_, sqlx::postgres::PgRow> for crate::models::post::Post {
    fn from_row(row: &sqlx::postgres::PgRow) -> sqlx::Result<Self> {
        Ok(crate::models::post::Post {
            id: row.try_get("id")?,
            title: row.try_get("title")?,
            body: row.try_get("body")?,
            author_id: row.try_get("authorId")?,
            author: None,
        })
    }
}

pub(crate) fn push_post_where(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    w: &crate::models::post::PostWhereInput,
) -> bool {
    let mut any = false;
    if let Some(f) = &w.id {
        if any {
            qb.push(" AND ");
        }
        any = true;
        crate::engine::sqlx_postgres::filters::push_string_filter(qb, "id", f);
    }
    if let Some(f) = &w.title {
        if any {
            qb.push(" AND ");
        }
        any = true;
        crate::engine::sqlx_postgres::filters::push_string_filter(qb, "title", f);
    }
    if let Some(f) = &w.body {
        if any {
            qb.push(" AND ");
        }
        any = true;
        crate::engine::sqlx_postgres::filters::push_string_nullable_filter(qb, "body", f);
    }
    if let Some(f) = &w.author_id {
        if any {
            qb.push(" AND ");
        }
        any = true;
        crate::engine::sqlx_postgres::filters::push_string_filter(qb, "authorId", f);
    }
    if let Some(rel) = &w.author {
        if let Some(sub) = &rel.is {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(
                "EXISTS (SELECT 1 FROM \"User\" t WHERE t.\"id\" = \"Post\".\"authorId\" AND (",
            );
            if !crate::engine::sqlx_postgres::models::user::push_user_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
        if let Some(sub) = &rel.is_not {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(
                "NOT EXISTS (SELECT 1 FROM \"User\" t WHERE t.\"id\" = \"Post\".\"authorId\" AND (",
            );
            if !crate::engine::sqlx_postgres::models::user::push_user_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
    }
    if let Some(group) = &w.and {
        for sub in group {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("(");
            if !push_post_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push(")");
        }
    }
    if let Some(group) = &w.or {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("(");
        let mut first = true;
        for sub in group {
            if !first {
                qb.push(" OR ");
            }
            first = false;
            qb.push("(");
            if !push_post_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push(")");
        }
        qb.push(")");
    }
    if let Some(group) = &w.not {
        for sub in group {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("NOT (");
            if !push_post_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push(")");
        }
    }
    any
}

impl crate::models::post::Post {
    pub async fn find_unique<'e, E>(
        executor: E,
        w: &crate::models::post::PostWhereUniqueInput,
    ) -> sqlx::Result<Option<Self>>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let cols = "\"id\", \"title\", \"body\", \"authorId\"";
        let mut qb = sqlx::QueryBuilder::new(format!(r#"SELECT {} FROM "Post" WHERE "#, cols,));
        // For v1, support only the @id field. Composite uniques are out of scope.
        // The WhereUniqueInput has Option<T> per @id/@unique field; we pick the
        // first Some(). Multi-Some inputs cover composite uniques — TODO Task v2.
        if let Some(value) = &w.id {
            qb.push("\"id\" = ");
            qb.push_bind(value.clone());
        } else {
            return Ok(None);
        }
        qb.build_query_as::<Self>().fetch_optional(executor).await
    }
}

impl crate::models::post::Post {
    pub async fn find_first<'e, E>(
        executor: E,
        w: &crate::models::post::PostWhereInput,
    ) -> sqlx::Result<Option<Self>>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut qb = sqlx::QueryBuilder::new(
            r#"SELECT "id", "title", "body", "authorId" FROM "Post" WHERE "#,
        );
        if !crate::engine::sqlx_postgres::models::post::push_post_where(&mut qb, w) {
            qb.push("TRUE");
        }
        qb.push(" LIMIT 1");
        qb.build_query_as::<Self>().fetch_optional(executor).await
    }
}

pub struct PostFindManyBuilder<'a, E> {
    executor: E,
    where_input: &'a crate::models::post::PostWhereInput,
    order_by: Vec<crate::models::post::PostOrderByWithRelationInput>,
    take: Option<i64>,
    skip: Option<i64>,
}

impl<'a, E> PostFindManyBuilder<'a, E> {
    pub fn order_by(
        mut self,
        order_by: &[crate::models::post::PostOrderByWithRelationInput],
    ) -> Self {
        self.order_by = order_by.to_vec();
        self
    }

    pub fn take(mut self, n: i64) -> Self {
        self.take = Some(n);
        self
    }

    pub fn skip(mut self, n: i64) -> Self {
        self.skip = Some(n);
        self
    }

    pub async fn exec<'e>(self) -> sqlx::Result<Vec<crate::models::post::Post>>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut qb = sqlx::QueryBuilder::new(
            r#"SELECT "id", "title", "body", "authorId" FROM "Post" WHERE "#,
        );
        if !crate::engine::sqlx_postgres::models::post::push_post_where(&mut qb, self.where_input) {
            qb.push("TRUE");
        }
        if !self.order_by.is_empty() {
            qb.push(" ORDER BY ");
            let mut first = true;
            for ob in &self.order_by {
                if let Some(o) = ob.id {
                    if !first {
                        qb.push(", ");
                    }
                    first = false;
                    qb.push("\"id\" ");
                    qb.push(crate::engine::sqlx_postgres::filters::sort_order_sql(o));
                }
                if let Some(o) = ob.title {
                    if !first {
                        qb.push(", ");
                    }
                    first = false;
                    qb.push("\"title\" ");
                    qb.push(crate::engine::sqlx_postgres::filters::sort_order_sql(o));
                }
                if let Some(o) = ob.body {
                    if !first {
                        qb.push(", ");
                    }
                    first = false;
                    qb.push("\"body\" ");
                    qb.push(crate::engine::sqlx_postgres::filters::sort_order_sql(o));
                }
                if let Some(o) = ob.author_id {
                    if !first {
                        qb.push(", ");
                    }
                    first = false;
                    qb.push("\"authorId\" ");
                    qb.push(crate::engine::sqlx_postgres::filters::sort_order_sql(o));
                }
                // TODO(prisma-rust-generator): sorting by relation `author` not yet supported.
                let _ = &ob.author;
            }
        }
        if let Some(n) = self.take {
            qb.push(" LIMIT ");
            qb.push_bind(n);
        }
        if let Some(n) = self.skip {
            qb.push(" OFFSET ");
            qb.push_bind(n);
        }
        qb.build_query_as::<crate::models::post::Post>()
            .fetch_all(self.executor)
            .await
    }
}

impl crate::models::post::Post {
    pub fn find_many<'a, E>(
        executor: E,
        w: &'a crate::models::post::PostWhereInput,
    ) -> PostFindManyBuilder<'a, E> {
        PostFindManyBuilder {
            executor,
            where_input: w,
            order_by: Vec::new(),
            take: None,
            skip: None,
        }
    }
}

impl crate::models::post::Post {
    pub async fn count<'e, E>(
        executor: E,
        w: Option<&crate::models::post::PostWhereInput>,
    ) -> sqlx::Result<i64>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut qb = sqlx::QueryBuilder::new(r#"SELECT COUNT(*) FROM "Post" WHERE "#);
        if let Some(w) = w {
            if !crate::engine::sqlx_postgres::models::post::push_post_where(&mut qb, w) {
                qb.push("TRUE");
            }
        } else {
            qb.push("TRUE");
        }
        let row = qb.build_query_as::<(i64,)>().fetch_one(executor).await?;
        Ok(row.0)
    }
}

impl crate::models::post::Post {
    pub async fn create<'e, E>(
        executor: E,
        input: crate::models::post::PostUncheckedCreateInput,
    ) -> sqlx::Result<Self>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut cols = String::new();
        cols.push_str("\"title\"");
        cols.push_str(", ");
        cols.push_str("\"authorId\"");
        if input.body.is_some() {
            cols.push_str(", ");
            cols.push_str("\"body\"");
        }
        let mut qb = sqlx::QueryBuilder::new(format!(r#"INSERT INTO "Post" ({}) VALUES ("#, cols,));
        qb.push_bind(input.title);
        qb.push(", ");
        qb.push_bind(input.author_id);
        if let Some(v) = input.body {
            qb.push(", ");
            qb.push_bind(v);
        }
        qb.push(r#") RETURNING "id", "title", "body", "authorId""#);
        qb.build_query_as::<Self>().fetch_one(executor).await
    }
}

impl crate::models::post::Post {
    pub async fn create_many<'e, E>(
        executor: E,
        inputs: &[crate::models::post::PostUncheckedCreateInput],
    ) -> sqlx::Result<u64>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        if inputs.is_empty() {
            return Ok(0);
        }
        let mut qb =
            sqlx::QueryBuilder::new(r#"INSERT INTO "Post" ("title", "body", "authorId") "#);
        qb.push_values(inputs, |mut row, input| {
            row.push_bind(input.title.clone());
            row.push_bind(input.body.clone());
            row.push_bind(input.author_id.clone());
        });
        let result = qb.build().execute(executor).await?;
        Ok(result.rows_affected())
    }
}

impl crate::models::post::Post {
    pub async fn update<'e, E>(
        executor: E,
        w: &crate::models::post::PostWhereUniqueInput,
        input: crate::models::post::PostUncheckedUpdateInput,
    ) -> sqlx::Result<Self>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut qb = sqlx::QueryBuilder::new(r#"UPDATE "Post" SET "#);
        let mut first = true;
        if let Some(op) = input.id {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"id\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.title {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"title\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.body {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"body\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.author_id {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"authorId\" = ");
                qb.push_bind(v);
            }
        }
        if first {
            // Nothing to update — fall back to a plain find_unique so the
            // caller still gets the row back (or RowNotFound if absent).
            return Self::find_unique(executor, w)
                .await?
                .ok_or(sqlx::Error::RowNotFound);
        }
        qb.push(" WHERE ");
        if let Some(value) = &w.id {
            qb.push("\"id\" = ");
            qb.push_bind(value.clone());
        } else {
            return Err(sqlx::Error::RowNotFound);
        }
        qb.push(r#" RETURNING "id", "title", "body", "authorId""#);
        qb.build_query_as::<Self>().fetch_one(executor).await
    }
}

impl crate::models::post::Post {
    pub async fn update_many<'e, E>(
        executor: E,
        w: &crate::models::post::PostWhereInput,
        input: crate::models::post::PostUncheckedUpdateManyInput,
    ) -> sqlx::Result<u64>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut qb = sqlx::QueryBuilder::new(r#"UPDATE "Post" SET "#);
        let mut first = true;
        if let Some(op) = input.id {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"id\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.title {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"title\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.body {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"body\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.author_id {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"authorId\" = ");
                qb.push_bind(v);
            }
        }
        if first {
            // No-op update — return 0 rows affected without issuing SQL.
            return Ok(0);
        }
        qb.push(" WHERE ");
        if !crate::engine::sqlx_postgres::models::post::push_post_where(&mut qb, w) {
            qb.push("TRUE");
        }
        let result = qb.build().execute(executor).await?;
        Ok(result.rows_affected())
    }
}

impl crate::models::post::Post {
    pub async fn delete<'e, E>(
        executor: E,
        w: &crate::models::post::PostWhereUniqueInput,
    ) -> sqlx::Result<Self>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut qb = sqlx::QueryBuilder::new(r#"DELETE FROM "Post" WHERE "#);
        if let Some(value) = &w.id {
            qb.push("\"id\" = ");
            qb.push_bind(value.clone());
        } else {
            return Err(sqlx::Error::RowNotFound);
        }
        qb.push(r#" RETURNING "id", "title", "body", "authorId""#);
        qb.build_query_as::<Self>().fetch_one(executor).await
    }
}

impl crate::models::post::Post {
    pub async fn delete_many<'e, E>(
        executor: E,
        w: &crate::models::post::PostWhereInput,
    ) -> sqlx::Result<u64>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut qb = sqlx::QueryBuilder::new(r#"DELETE FROM "Post" WHERE "#);
        if !crate::engine::sqlx_postgres::models::post::push_post_where(&mut qb, w) {
            qb.push("TRUE");
        }
        let result = qb.build().execute(executor).await?;
        Ok(result.rows_affected())
    }
}

#[derive(Debug, Clone, Default, PartialEq, Eq, Hash, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct PostCountAggregateResult {
    pub id: Option<i64>,
    pub title: Option<i64>,
    pub body: Option<i64>,
    pub author_id: Option<i64>,
    #[serde(rename = "_all")]
    pub aggregate_all: Option<i64>,
}

#[derive(Debug, Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct PostAvgAggregateResult {}

#[derive(Debug, Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct PostSumAggregateResult {}

#[derive(Debug, Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct PostMinAggregateResult {
    pub id: Option<String>,
    pub title: Option<String>,
    pub body: Option<String>,
    pub author_id: Option<String>,
}

#[derive(Debug, Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct PostMaxAggregateResult {
    pub id: Option<String>,
    pub title: Option<String>,
    pub body: Option<String>,
    pub author_id: Option<String>,
}

#[derive(Debug, Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct PostAggregateResult {
    #[serde(rename = "_count")]
    pub aggregate_count: Option<PostCountAggregateResult>,
    #[serde(rename = "_avg")]
    pub aggregate_avg: Option<PostAvgAggregateResult>,
    #[serde(rename = "_sum")]
    pub aggregate_sum: Option<PostSumAggregateResult>,
    #[serde(rename = "_min")]
    pub aggregate_min: Option<PostMinAggregateResult>,
    #[serde(rename = "_max")]
    pub aggregate_max: Option<PostMaxAggregateResult>,
}

impl crate::models::post::Post {
    pub async fn aggregate<'e, E>(
        executor: E,
        input: &crate::models::post::PostAggregateInput,
    ) -> sqlx::Result<PostAggregateResult>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut qb = sqlx::QueryBuilder::new(
            r#"SELECT COUNT(*) AS count_all, COUNT("id") AS count_id, COUNT("title") AS count_title, COUNT("body") AS count_body, COUNT("authorId") AS count_author_id, MIN("id") AS min_id, MAX("id") AS max_id, MIN("title") AS min_title, MAX("title") AS max_title, MIN("body") AS min_body, MAX("body") AS max_body, MIN("authorId") AS min_author_id, MAX("authorId") AS max_author_id FROM "Post" WHERE "#,
        );
        if let Some(w) = &input.r#where {
            if !crate::engine::sqlx_postgres::models::post::push_post_where(&mut qb, w) {
                qb.push("TRUE");
            }
        } else {
            qb.push("TRUE");
        }
        let row = qb.build().fetch_one(executor).await?;
        use sqlx::Row;
        Ok(PostAggregateResult {
            aggregate_count: input
                .aggregate_count
                .as_ref()
                .map(|req| -> sqlx::Result<_> {
                    Ok(PostCountAggregateResult {
                        id: if req.id.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_id")?
                        } else {
                            None
                        },
                        title: if req.title.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_title")?
                        } else {
                            None
                        },
                        body: if req.body.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_body")?
                        } else {
                            None
                        },
                        author_id: if req.author_id.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_author_id")?
                        } else {
                            None
                        },
                        aggregate_all: if req.aggregate_all.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_all")?
                        } else {
                            None
                        },
                    })
                })
                .transpose()?,
            aggregate_avg: input
                .aggregate_avg
                .as_ref()
                .map(|_req| -> sqlx::Result<_> { Ok(PostAvgAggregateResult {}) })
                .transpose()?,
            aggregate_sum: input
                .aggregate_sum
                .as_ref()
                .map(|_req| -> sqlx::Result<_> { Ok(PostSumAggregateResult {}) })
                .transpose()?,
            aggregate_min: input
                .aggregate_min
                .as_ref()
                .map(|req| -> sqlx::Result<_> {
                    Ok(PostMinAggregateResult {
                        id: if req.id.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("min_id")?
                        } else {
                            None
                        },
                        title: if req.title.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("min_title")?
                        } else {
                            None
                        },
                        body: if req.body.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("min_body")?
                        } else {
                            None
                        },
                        author_id: if req.author_id.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("min_author_id")?
                        } else {
                            None
                        },
                    })
                })
                .transpose()?,
            aggregate_max: input
                .aggregate_max
                .as_ref()
                .map(|req| -> sqlx::Result<_> {
                    Ok(PostMaxAggregateResult {
                        id: if req.id.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("max_id")?
                        } else {
                            None
                        },
                        title: if req.title.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("max_title")?
                        } else {
                            None
                        },
                        body: if req.body.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("max_body")?
                        } else {
                            None
                        },
                        author_id: if req.author_id.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("max_author_id")?
                        } else {
                            None
                        },
                    })
                })
                .transpose()?,
        })
    }
}

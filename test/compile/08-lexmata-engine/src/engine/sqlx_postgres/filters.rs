// @generated by prisma-rust-generator — do not edit by hand
#![allow(clippy::upper_case_acronyms, dead_code)]
// engine/sqlx_postgres/filters.rs

// @generated by prisma-rust-generator
#![allow(clippy::too_many_lines)]
#![allow(unused_imports)]

use crate::shared::filters;
use crate::shared::filters::QueryMode;

pub(crate) fn push_uuid_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &filters::UuidFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_uuid_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_uuid_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &filters::UuidNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_uuid_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_string_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &filters::StringFilter,
) -> bool {
    qb.push("(");
    let mut any = false;
    let ci = matches!(f.mode, Some(QueryMode::Insensitive));
    let like_op = if ci { "ILIKE" } else { "LIKE" };

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if ci {
            qb.push(format!("LOWER({col}) = LOWER("));
            qb.push_bind(value.clone());
            qb.push(")");
        } else {
            qb.push(format!("{col} = "));
            qb.push_bind(value.clone());
        }
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(value) = &f.lt {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} < "));
        qb.push_bind(value.clone());
    }
    if let Some(value) = &f.lte {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} <= "));
        qb.push_bind(value.clone());
    }
    if let Some(value) = &f.gt {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} > "));
        qb.push_bind(value.clone());
    }
    if let Some(value) = &f.gte {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} >= "));
        qb.push_bind(value.clone());
    }
    if let Some(value) = &f.contains {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} {like_op} "));
        qb.push_bind(format!("%{value}%"));
    }
    if let Some(value) = &f.starts_with {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} {like_op} "));
        qb.push_bind(format!("{value}%"));
    }
    if let Some(value) = &f.ends_with {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} {like_op} "));
        qb.push_bind(format!("%{value}"));
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_string_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_string_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &filters::StringNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;
    let ci = matches!(f.mode, Some(QueryMode::Insensitive));
    let like_op = if ci { "ILIKE" } else { "LIKE" };

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if ci {
            qb.push(format!("LOWER({col}) = LOWER("));
            qb.push_bind(value.clone());
            qb.push(")");
        } else {
            qb.push(format!("{col} = "));
            qb.push_bind(value.clone());
        }
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(value) = &f.lt {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} < "));
        qb.push_bind(value.clone());
    }
    if let Some(value) = &f.lte {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} <= "));
        qb.push_bind(value.clone());
    }
    if let Some(value) = &f.gt {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} > "));
        qb.push_bind(value.clone());
    }
    if let Some(value) = &f.gte {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} >= "));
        qb.push_bind(value.clone());
    }
    if let Some(value) = &f.contains {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} {like_op} "));
        qb.push_bind(format!("%{value}%"));
    }
    if let Some(value) = &f.starts_with {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} {like_op} "));
        qb.push_bind(format!("{value}%"));
    }
    if let Some(value) = &f.ends_with {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} {like_op} "));
        qb.push_bind(format!("%{value}"));
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_string_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_int_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &filters::IntFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(value) = &f.lt {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} < "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.lte {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} <= "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.gt {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} > "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.gte {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} >= "));
        qb.push_bind(*value);
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_int_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_int_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &filters::IntNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(value) = &f.lt {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} < "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.lte {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} <= "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.gt {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} > "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.gte {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} >= "));
        qb.push_bind(*value);
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_int_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_bigint_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &filters::BigIntFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(value) = &f.lt {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} < "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.lte {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} <= "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.gt {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} > "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.gte {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} >= "));
        qb.push_bind(*value);
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_bigint_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_bigint_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &filters::BigIntNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(value) = &f.lt {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} < "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.lte {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} <= "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.gt {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} > "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.gte {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} >= "));
        qb.push_bind(*value);
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_bigint_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_float_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &filters::FloatFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(value) = &f.lt {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} < "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.lte {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} <= "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.gt {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} > "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.gte {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} >= "));
        qb.push_bind(*value);
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_float_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_float_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &filters::FloatNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(value) = &f.lt {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} < "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.lte {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} <= "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.gt {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} > "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.gte {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} >= "));
        qb.push_bind(*value);
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_float_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_decimal_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &filters::DecimalFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(value) = &f.lt {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} < "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.lte {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} <= "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.gt {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} > "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.gte {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} >= "));
        qb.push_bind(*value);
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_decimal_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_decimal_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &filters::DecimalNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(value) = &f.lt {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} < "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.lte {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} <= "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.gt {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} > "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.gte {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} >= "));
        qb.push_bind(*value);
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_decimal_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_datetime_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &filters::DateTimeFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(value) = &f.lt {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} < "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.lte {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} <= "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.gt {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} > "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.gte {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} >= "));
        qb.push_bind(*value);
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_datetime_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_datetime_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &filters::DateTimeNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(value) = &f.lt {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} < "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.lte {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} <= "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.gt {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} > "));
        qb.push_bind(*value);
    }
    if let Some(value) = &f.gte {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} >= "));
        qb.push_bind(*value);
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_datetime_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_bool_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &filters::BoolFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_bool_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_bool_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &filters::BoolNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_bool_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_bytes_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &filters::BytesFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(value.clone());
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_bytes_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_bytes_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &filters::BytesNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(value.clone());
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_bytes_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_json_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &filters::JsonFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(value.clone());
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_json_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_json_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &filters::JsonNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(value.clone());
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_json_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_advita_record_type_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::advita::AdvitaRecordTypeFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_advita_record_type_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_advita_record_type_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::advita::AdvitaRecordTypeNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_advita_record_type_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_advita_sync_status_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::advita::AdvitaSyncStatusFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_advita_sync_status_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_advita_sync_status_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::advita::AdvitaSyncStatusNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_advita_sync_status_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_advita_tracking_status_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::advita::AdvitaTrackingStatusFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_advita_tracking_status_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_advita_tracking_status_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::advita::AdvitaTrackingStatusNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_advita_tracking_status_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_email_verification_type_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::auth::EmailVerificationTypeFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_email_verification_type_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_email_verification_type_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::auth::EmailVerificationTypeNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_email_verification_type_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_invoice_status_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::billing::InvoiceStatusFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_invoice_status_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_invoice_status_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::billing::InvoiceStatusNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_invoice_status_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_processing_status_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::case_processing::ProcessingStatusFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_processing_status_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_processing_status_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::case_processing::ProcessingStatusNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_processing_status_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_case_review_audience_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::case_processing::CaseReviewAudienceFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_case_review_audience_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_case_review_audience_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::case_processing::CaseReviewAudienceNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_case_review_audience_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_processing_tier_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::case_processing::ProcessingTierFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_processing_tier_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_processing_tier_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::case_processing::ProcessingTierNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_processing_tier_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_tort_type_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::cases::TortTypeFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_tort_type_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_tort_type_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::cases::TortTypeNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_tort_type_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_case_status_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::cases::CaseStatusFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_case_status_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_case_status_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::cases::CaseStatusNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_case_status_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_treatment_status_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::cases::TreatmentStatusFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_treatment_status_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_treatment_status_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::cases::TreatmentStatusNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_treatment_status_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_coverage_type_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::cases::CoverageTypeFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_coverage_type_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_coverage_type_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::cases::CoverageTypeNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_coverage_type_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_opposing_party_type_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::cases::OpposingPartyTypeFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_opposing_party_type_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_opposing_party_type_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::cases::OpposingPartyTypeNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_opposing_party_type_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_case_task_status_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::cases::CaseTaskStatusFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_case_task_status_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_case_task_status_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::cases::CaseTaskStatusNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_case_task_status_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_case_task_priority_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::cases::CaseTaskPriorityFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_case_task_priority_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_case_task_priority_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::cases::CaseTaskPriorityNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_case_task_priority_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_calendar_entry_type_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::cases::CalendarEntryTypeFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_calendar_entry_type_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_calendar_entry_type_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::cases::CalendarEntryTypeNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_calendar_entry_type_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_contact_role_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::cases::ContactRoleFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_contact_role_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_contact_role_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::cases::ContactRoleNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_contact_role_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_billing_expense_category_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::cases::BillingExpenseCategoryFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_billing_expense_category_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_billing_expense_category_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::cases::BillingExpenseCategoryNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_billing_expense_category_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_field_review_status_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::documents::FieldReviewStatusFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_field_review_status_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_field_review_status_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::documents::FieldReviewStatusNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_field_review_status_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_document_type_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::documents::DocumentTypeFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_document_type_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_document_type_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::documents::DocumentTypeNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_document_type_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_external_platform_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::external_platforms::ExternalPlatformFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_external_platform_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_external_platform_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::external_platforms::ExternalPlatformNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_external_platform_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_external_sync_status_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::external_platforms::ExternalSyncStatusFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_external_sync_status_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_external_sync_status_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::external_platforms::ExternalSyncStatusNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_external_sync_status_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_external_sync_frequency_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::external_platforms::ExternalSyncFrequencyFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_external_sync_frequency_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_external_sync_frequency_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::external_platforms::ExternalSyncFrequencyNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_external_sync_frequency_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_sso_provider_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::firms::SSOProviderFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_sso_provider_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_sso_provider_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::firms::SSOProviderNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_sso_provider_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_form_template_status_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::forms::FormTemplateStatusFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_form_template_status_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_form_template_status_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::forms::FormTemplateStatusNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_form_template_status_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_form_field_type_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::forms::FormFieldTypeFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_form_field_type_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_form_field_type_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::forms::FormFieldTypeNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_form_field_type_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_distribution_status_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::forms::DistributionStatusFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_distribution_status_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_distribution_status_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::forms::DistributionStatusNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_distribution_status_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_submission_status_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::forms::SubmissionStatusFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_submission_status_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_submission_status_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::forms::SubmissionStatusNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_submission_status_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_notification_type_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::notifications::NotificationTypeFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_notification_type_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_notification_type_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::notifications::NotificationTypeNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_notification_type_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_delivery_status_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::notifications::DeliveryStatusFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_delivery_status_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_delivery_status_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::notifications::DeliveryStatusNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_delivery_status_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_pdf_processing_status_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::pdf_processing::PdfProcessingStatusFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_pdf_processing_status_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_pdf_processing_status_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::pdf_processing::PdfProcessingStatusNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_pdf_processing_status_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_standalone_ocr_job_status_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::pdf_processing::StandaloneOcrJobStatusFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_standalone_ocr_job_status_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_standalone_ocr_job_status_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::pdf_processing::StandaloneOcrJobStatusNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_standalone_ocr_job_status_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_qualification_criteria_status_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::qualification::QualificationCriteriaStatusFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_qualification_criteria_status_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_qualification_criteria_status_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::qualification::QualificationCriteriaStatusNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_qualification_criteria_status_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_qualification_verdict_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::qualification::QualificationVerdictFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_qualification_verdict_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_qualification_verdict_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::qualification::QualificationVerdictNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_qualification_verdict_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_tort_template_status_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::qualification::TortTemplateStatusFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_tort_template_status_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_tort_template_status_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::qualification::TortTemplateStatusNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_tort_template_status_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_fee_payer_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::reference::FeePayerFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_fee_payer_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_fee_payer_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::reference::FeePayerNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_fee_payer_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_subscription_tier_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::users::SubscriptionTierFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_subscription_tier_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn push_subscription_tier_nullable_filter(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    col: &str,
    f: &crate::users::SubscriptionTierNullableFilter,
) -> bool {
    qb.push("(");
    let mut any = false;

    if let Some(want_null) = f.is_null {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if want_null {
            qb.push(format!("{col} IS NULL"));
        } else {
            qb.push(format!("{col} IS NOT NULL"));
        }
    }
    if let Some(value) = &f.equals {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push(format!("{col} = "));
        qb.push_bind(*value);
    }
    if let Some(values) = &f.r#in {
        if any {
            qb.push(" AND ");
        }
        any = true;
        if values.is_empty() {
            qb.push("FALSE");
        } else {
            qb.push(format!("{col} = ANY("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(values) = &f.not_in {
        if !values.is_empty() {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push(format!("{col} <> ALL("));
            qb.push_bind(values.clone());
            qb.push(")");
        }
    }
    if let Some(inner) = &f.not {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("NOT ");
        push_subscription_tier_nullable_filter(qb, col, inner);
    }

    if !any {
        qb.push("TRUE");
    }
    qb.push(")");
    any
}

pub(crate) fn sort_order_sql(o: crate::shared::filters::SortOrder) -> &'static str {
    match o {
        crate::shared::filters::SortOrder::Asc => "ASC",
        crate::shared::filters::SortOrder::Desc => "DESC",
        crate::shared::filters::SortOrder::AscNullsFirst => "ASC NULLS FIRST",
        crate::shared::filters::SortOrder::AscNullsLast => "ASC NULLS LAST",
        crate::shared::filters::SortOrder::DescNullsFirst => "DESC NULLS FIRST",
        crate::shared::filters::SortOrder::DescNullsLast => "DESC NULLS LAST",
    }
}

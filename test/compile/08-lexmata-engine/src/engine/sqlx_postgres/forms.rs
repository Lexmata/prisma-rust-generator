// @generated by prisma-rust-generator — do not edit by hand
#![allow(clippy::upper_case_acronyms, dead_code)]

use sqlx::Row as _;

impl sqlx::Type<sqlx::Postgres> for crate::forms::FormTemplateStatus {
    fn type_info() -> sqlx::postgres::PgTypeInfo {
        sqlx::postgres::PgTypeInfo::with_name("FormTemplateStatus")
    }
    fn compatible(ty: &sqlx::postgres::PgTypeInfo) -> bool {
        *ty == Self::type_info() || <&str as sqlx::Type<sqlx::Postgres>>::compatible(ty)
    }
}

impl<'r> sqlx::Decode<'r, sqlx::Postgres> for crate::forms::FormTemplateStatus {
    fn decode(value: sqlx::postgres::PgValueRef<'r>) -> Result<Self, sqlx::error::BoxDynError> {
        let s = <&str as sqlx::Decode<sqlx::Postgres>>::decode(value)?;
        match s {
            "DRAFT" => Ok(crate::forms::FormTemplateStatus::Draft),
            "PUBLISHED" => Ok(crate::forms::FormTemplateStatus::Published),
            "ARCHIVED" => Ok(crate::forms::FormTemplateStatus::Archived),
            other => Err(format!("unknown FormTemplateStatus variant: {other}").into()),
        }
    }
}

impl<'q> sqlx::Encode<'q, sqlx::Postgres> for crate::forms::FormTemplateStatus {
    fn encode_by_ref(
        &self,
        buf: &mut sqlx::postgres::PgArgumentBuffer,
    ) -> Result<sqlx::encode::IsNull, sqlx::error::BoxDynError> {
        let s: &'static str = match self {
            crate::forms::FormTemplateStatus::Draft => "DRAFT",
            crate::forms::FormTemplateStatus::Published => "PUBLISHED",
            crate::forms::FormTemplateStatus::Archived => "ARCHIVED",
        };
        <&str as sqlx::Encode<sqlx::Postgres>>::encode(s, buf)
    }
}

impl sqlx::postgres::PgHasArrayType for crate::forms::FormTemplateStatus {
    fn array_type_info() -> sqlx::postgres::PgTypeInfo {
        sqlx::postgres::PgTypeInfo::with_name("_FormTemplateStatus")
    }
}

impl sqlx::Type<sqlx::Postgres> for crate::forms::FormFieldType {
    fn type_info() -> sqlx::postgres::PgTypeInfo {
        sqlx::postgres::PgTypeInfo::with_name("FormFieldType")
    }
    fn compatible(ty: &sqlx::postgres::PgTypeInfo) -> bool {
        *ty == Self::type_info() || <&str as sqlx::Type<sqlx::Postgres>>::compatible(ty)
    }
}

impl<'r> sqlx::Decode<'r, sqlx::Postgres> for crate::forms::FormFieldType {
    fn decode(value: sqlx::postgres::PgValueRef<'r>) -> Result<Self, sqlx::error::BoxDynError> {
        let s = <&str as sqlx::Decode<sqlx::Postgres>>::decode(value)?;
        match s {
            "TEXT" => Ok(crate::forms::FormFieldType::Text),
            "TEXTAREA" => Ok(crate::forms::FormFieldType::Textarea),
            "NUMBER" => Ok(crate::forms::FormFieldType::Number),
            "DATE" => Ok(crate::forms::FormFieldType::Date),
            "EMAIL" => Ok(crate::forms::FormFieldType::Email),
            "PHONE" => Ok(crate::forms::FormFieldType::Phone),
            "CHECKBOX" => Ok(crate::forms::FormFieldType::Checkbox),
            "RADIO" => Ok(crate::forms::FormFieldType::Radio),
            "DROPDOWN" => Ok(crate::forms::FormFieldType::Dropdown),
            "FILE_UPLOAD" => Ok(crate::forms::FormFieldType::FileUpload),
            "SIGNATURE" => Ok(crate::forms::FormFieldType::Signature),
            "ADDRESS" => Ok(crate::forms::FormFieldType::Address),
            "RICH_TEXT" => Ok(crate::forms::FormFieldType::RichText),
            "SECTION_HEADER" => Ok(crate::forms::FormFieldType::SectionHeader),
            other => Err(format!("unknown FormFieldType variant: {other}").into()),
        }
    }
}

impl<'q> sqlx::Encode<'q, sqlx::Postgres> for crate::forms::FormFieldType {
    fn encode_by_ref(
        &self,
        buf: &mut sqlx::postgres::PgArgumentBuffer,
    ) -> Result<sqlx::encode::IsNull, sqlx::error::BoxDynError> {
        let s: &'static str = match self {
            crate::forms::FormFieldType::Text => "TEXT",
            crate::forms::FormFieldType::Textarea => "TEXTAREA",
            crate::forms::FormFieldType::Number => "NUMBER",
            crate::forms::FormFieldType::Date => "DATE",
            crate::forms::FormFieldType::Email => "EMAIL",
            crate::forms::FormFieldType::Phone => "PHONE",
            crate::forms::FormFieldType::Checkbox => "CHECKBOX",
            crate::forms::FormFieldType::Radio => "RADIO",
            crate::forms::FormFieldType::Dropdown => "DROPDOWN",
            crate::forms::FormFieldType::FileUpload => "FILE_UPLOAD",
            crate::forms::FormFieldType::Signature => "SIGNATURE",
            crate::forms::FormFieldType::Address => "ADDRESS",
            crate::forms::FormFieldType::RichText => "RICH_TEXT",
            crate::forms::FormFieldType::SectionHeader => "SECTION_HEADER",
        };
        <&str as sqlx::Encode<sqlx::Postgres>>::encode(s, buf)
    }
}

impl sqlx::postgres::PgHasArrayType for crate::forms::FormFieldType {
    fn array_type_info() -> sqlx::postgres::PgTypeInfo {
        sqlx::postgres::PgTypeInfo::with_name("_FormFieldType")
    }
}

impl sqlx::Type<sqlx::Postgres> for crate::forms::DistributionStatus {
    fn type_info() -> sqlx::postgres::PgTypeInfo {
        sqlx::postgres::PgTypeInfo::with_name("DistributionStatus")
    }
    fn compatible(ty: &sqlx::postgres::PgTypeInfo) -> bool {
        *ty == Self::type_info() || <&str as sqlx::Type<sqlx::Postgres>>::compatible(ty)
    }
}

impl<'r> sqlx::Decode<'r, sqlx::Postgres> for crate::forms::DistributionStatus {
    fn decode(value: sqlx::postgres::PgValueRef<'r>) -> Result<Self, sqlx::error::BoxDynError> {
        let s = <&str as sqlx::Decode<sqlx::Postgres>>::decode(value)?;
        match s {
            "PENDING" => Ok(crate::forms::DistributionStatus::Pending),
            "OPENED" => Ok(crate::forms::DistributionStatus::Opened),
            "SUBMITTED" => Ok(crate::forms::DistributionStatus::Submitted),
            "EXPIRED" => Ok(crate::forms::DistributionStatus::Expired),
            "REVOKED" => Ok(crate::forms::DistributionStatus::Revoked),
            other => Err(format!("unknown DistributionStatus variant: {other}").into()),
        }
    }
}

impl<'q> sqlx::Encode<'q, sqlx::Postgres> for crate::forms::DistributionStatus {
    fn encode_by_ref(
        &self,
        buf: &mut sqlx::postgres::PgArgumentBuffer,
    ) -> Result<sqlx::encode::IsNull, sqlx::error::BoxDynError> {
        let s: &'static str = match self {
            crate::forms::DistributionStatus::Pending => "PENDING",
            crate::forms::DistributionStatus::Opened => "OPENED",
            crate::forms::DistributionStatus::Submitted => "SUBMITTED",
            crate::forms::DistributionStatus::Expired => "EXPIRED",
            crate::forms::DistributionStatus::Revoked => "REVOKED",
        };
        <&str as sqlx::Encode<sqlx::Postgres>>::encode(s, buf)
    }
}

impl sqlx::postgres::PgHasArrayType for crate::forms::DistributionStatus {
    fn array_type_info() -> sqlx::postgres::PgTypeInfo {
        sqlx::postgres::PgTypeInfo::with_name("_DistributionStatus")
    }
}

impl sqlx::Type<sqlx::Postgres> for crate::forms::SubmissionStatus {
    fn type_info() -> sqlx::postgres::PgTypeInfo {
        sqlx::postgres::PgTypeInfo::with_name("SubmissionStatus")
    }
    fn compatible(ty: &sqlx::postgres::PgTypeInfo) -> bool {
        *ty == Self::type_info() || <&str as sqlx::Type<sqlx::Postgres>>::compatible(ty)
    }
}

impl<'r> sqlx::Decode<'r, sqlx::Postgres> for crate::forms::SubmissionStatus {
    fn decode(value: sqlx::postgres::PgValueRef<'r>) -> Result<Self, sqlx::error::BoxDynError> {
        let s = <&str as sqlx::Decode<sqlx::Postgres>>::decode(value)?;
        match s {
            "SUBMITTED" => Ok(crate::forms::SubmissionStatus::Submitted),
            "UNDER_REVIEW" => Ok(crate::forms::SubmissionStatus::UnderReview),
            "VERIFIED" => Ok(crate::forms::SubmissionStatus::Verified),
            "REJECTED" => Ok(crate::forms::SubmissionStatus::Rejected),
            "RESUBMISSION_REQUESTED" => Ok(crate::forms::SubmissionStatus::ResubmissionRequested),
            other => Err(format!("unknown SubmissionStatus variant: {other}").into()),
        }
    }
}

impl<'q> sqlx::Encode<'q, sqlx::Postgres> for crate::forms::SubmissionStatus {
    fn encode_by_ref(
        &self,
        buf: &mut sqlx::postgres::PgArgumentBuffer,
    ) -> Result<sqlx::encode::IsNull, sqlx::error::BoxDynError> {
        let s: &'static str = match self {
            crate::forms::SubmissionStatus::Submitted => "SUBMITTED",
            crate::forms::SubmissionStatus::UnderReview => "UNDER_REVIEW",
            crate::forms::SubmissionStatus::Verified => "VERIFIED",
            crate::forms::SubmissionStatus::Rejected => "REJECTED",
            crate::forms::SubmissionStatus::ResubmissionRequested => "RESUBMISSION_REQUESTED",
        };
        <&str as sqlx::Encode<sqlx::Postgres>>::encode(s, buf)
    }
}

impl sqlx::postgres::PgHasArrayType for crate::forms::SubmissionStatus {
    fn array_type_info() -> sqlx::postgres::PgTypeInfo {
        sqlx::postgres::PgTypeInfo::with_name("_SubmissionStatus")
    }
}

impl sqlx::FromRow<'_, sqlx::postgres::PgRow> for crate::forms::FormTemplate {
    fn from_row(row: &sqlx::postgres::PgRow) -> sqlx::Result<Self> {
        Ok(crate::forms::FormTemplate {
            id: row.try_get("id")?,
            firm_id: row.try_get("firm_id")?,
            name: row.try_get("name")?,
            description: row.try_get("description")?,
            status: row.try_get("status")?,
            current_version: row.try_get("current_version")?,
            created_by_id: row.try_get("created_by_id")?,
            created_at: row.try_get("created_at")?,
            updated_at: row.try_get("updated_at")?,
            firm: None,
            created_by: None,
            versions: Vec::new(),
            distributions: Vec::new(),
            submissions: Vec::new(),
        })
    }
}

pub(crate) fn push_form_template_where(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    w: &crate::forms::FormTemplateWhereInput,
) -> bool {
    let mut any = false;
    if let Some(f) = &w.id {
        if any {
            qb.push(" AND ");
        }
        any = true;
        crate::engine::sqlx_postgres::filters::push_string_filter(qb, "id", f);
    }
    if let Some(f) = &w.firm_id {
        if any {
            qb.push(" AND ");
        }
        any = true;
        crate::engine::sqlx_postgres::filters::push_string_filter(qb, "firm_id", f);
    }
    if let Some(f) = &w.name {
        if any {
            qb.push(" AND ");
        }
        any = true;
        crate::engine::sqlx_postgres::filters::push_string_filter(qb, "name", f);
    }
    if let Some(f) = &w.description {
        if any {
            qb.push(" AND ");
        }
        any = true;
        crate::engine::sqlx_postgres::filters::push_string_nullable_filter(qb, "description", f);
    }
    if let Some(f) = &w.status {
        if any {
            qb.push(" AND ");
        }
        any = true;
        crate::engine::sqlx_postgres::filters::push_form_template_status_filter(qb, "status", f);
    }
    if let Some(f) = &w.current_version {
        if any {
            qb.push(" AND ");
        }
        any = true;
        crate::engine::sqlx_postgres::filters::push_int_filter(qb, "current_version", f);
    }
    if let Some(f) = &w.created_by_id {
        if any {
            qb.push(" AND ");
        }
        any = true;
        crate::engine::sqlx_postgres::filters::push_string_filter(qb, "created_by_id", f);
    }
    if let Some(f) = &w.created_at {
        if any {
            qb.push(" AND ");
        }
        any = true;
        crate::engine::sqlx_postgres::filters::push_datetime_filter(qb, "created_at", f);
    }
    if let Some(f) = &w.updated_at {
        if any {
            qb.push(" AND ");
        }
        any = true;
        crate::engine::sqlx_postgres::filters::push_datetime_filter(qb, "updated_at", f);
    }
    if let Some(rel) = &w.firm {
        if let Some(sub) = &rel.is {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("EXISTS (SELECT 1 FROM \"firms\" t WHERE t.\"id\" = \"form_templates\".\"firm_id\" AND (");
            if !crate::engine::sqlx_postgres::firms::push_firm_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
        if let Some(sub) = &rel.is_not {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("NOT EXISTS (SELECT 1 FROM \"firms\" t WHERE t.\"id\" = \"form_templates\".\"firm_id\" AND (");
            if !crate::engine::sqlx_postgres::firms::push_firm_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
    }
    if let Some(rel) = &w.created_by {
        if let Some(sub) = &rel.is {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("EXISTS (SELECT 1 FROM \"users\" t WHERE t.\"id\" = \"form_templates\".\"created_by_id\" AND (");
            if !crate::engine::sqlx_postgres::users::push_user_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
        if let Some(sub) = &rel.is_not {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("NOT EXISTS (SELECT 1 FROM \"users\" t WHERE t.\"id\" = \"form_templates\".\"created_by_id\" AND (");
            if !crate::engine::sqlx_postgres::users::push_user_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
    }
    if let Some(rel) = &w.versions {
        if let Some(sub) = &rel.every {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("NOT EXISTS (SELECT 1 FROM \"form_template_versions\" t WHERE t.\"template_id\" = \"form_templates\".\"id\" AND NOT (");
            if !crate::engine::sqlx_postgres::forms::push_form_template_version_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
        if let Some(sub) = &rel.some {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("EXISTS (SELECT 1 FROM \"form_template_versions\" t WHERE t.\"template_id\" = \"form_templates\".\"id\" AND (");
            if !crate::engine::sqlx_postgres::forms::push_form_template_version_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
        if let Some(sub) = &rel.none {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("NOT EXISTS (SELECT 1 FROM \"form_template_versions\" t WHERE t.\"template_id\" = \"form_templates\".\"id\" AND (");
            if !crate::engine::sqlx_postgres::forms::push_form_template_version_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
    }
    if let Some(rel) = &w.distributions {
        if let Some(sub) = &rel.every {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("NOT EXISTS (SELECT 1 FROM \"form_distributions\" t WHERE t.\"template_id\" = \"form_templates\".\"id\" AND NOT (");
            if !crate::engine::sqlx_postgres::forms::push_form_distribution_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
        if let Some(sub) = &rel.some {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("EXISTS (SELECT 1 FROM \"form_distributions\" t WHERE t.\"template_id\" = \"form_templates\".\"id\" AND (");
            if !crate::engine::sqlx_postgres::forms::push_form_distribution_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
        if let Some(sub) = &rel.none {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("NOT EXISTS (SELECT 1 FROM \"form_distributions\" t WHERE t.\"template_id\" = \"form_templates\".\"id\" AND (");
            if !crate::engine::sqlx_postgres::forms::push_form_distribution_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
    }
    if let Some(rel) = &w.submissions {
        if let Some(sub) = &rel.every {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("NOT EXISTS (SELECT 1 FROM \"form_submissions\" t WHERE t.\"template_id\" = \"form_templates\".\"id\" AND NOT (");
            if !crate::engine::sqlx_postgres::forms::push_form_submission_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
        if let Some(sub) = &rel.some {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("EXISTS (SELECT 1 FROM \"form_submissions\" t WHERE t.\"template_id\" = \"form_templates\".\"id\" AND (");
            if !crate::engine::sqlx_postgres::forms::push_form_submission_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
        if let Some(sub) = &rel.none {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("NOT EXISTS (SELECT 1 FROM \"form_submissions\" t WHERE t.\"template_id\" = \"form_templates\".\"id\" AND (");
            if !crate::engine::sqlx_postgres::forms::push_form_submission_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
    }
    if let Some(group) = &w.and {
        for sub in group {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("(");
            if !push_form_template_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push(")");
        }
    }
    if let Some(group) = &w.or {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("(");
        let mut first = true;
        for sub in group {
            if !first {
                qb.push(" OR ");
            }
            first = false;
            qb.push("(");
            if !push_form_template_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push(")");
        }
        qb.push(")");
    }
    if let Some(group) = &w.not {
        for sub in group {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("NOT (");
            if !push_form_template_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push(")");
        }
    }
    any
}

impl crate::forms::FormTemplate {
    pub async fn find_unique<'e, E>(
        executor: E,
        w: &crate::forms::FormTemplateWhereUniqueInput,
    ) -> sqlx::Result<Option<Self>>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let cols = "\"id\", \"firm_id\", \"name\", \"description\", \"status\", \"current_version\", \"created_by_id\", \"created_at\", \"updated_at\"";
        let mut qb =
            sqlx::QueryBuilder::new(format!(r#"SELECT {} FROM "form_templates" WHERE "#, cols,));
        // For v1, support only the @id field. Composite uniques are out of scope.
        // The WhereUniqueInput has Option<T> per @id/@unique field; we pick the
        // first Some(). Multi-Some inputs cover composite uniques — TODO Task v2.
        if let Some(value) = &w.id {
            qb.push("\"id\" = ");
            qb.push_bind(value.clone());
        } else {
            return Ok(None);
        }
        qb.build_query_as::<Self>().fetch_optional(executor).await
    }
}

impl crate::forms::FormTemplate {
    pub async fn find_first<'e, E>(
        executor: E,
        w: &crate::forms::FormTemplateWhereInput,
    ) -> sqlx::Result<Option<Self>>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut qb = sqlx::QueryBuilder::new(
            r#"SELECT "id", "firm_id", "name", "description", "status", "current_version", "created_by_id", "created_at", "updated_at" FROM "form_templates" WHERE "#,
        );
        if !crate::engine::sqlx_postgres::forms::push_form_template_where(&mut qb, w) {
            qb.push("TRUE");
        }
        qb.push(" LIMIT 1");
        qb.build_query_as::<Self>().fetch_optional(executor).await
    }
}

pub struct FormTemplateFindManyBuilder<'a, E> {
    executor: E,
    where_input: &'a crate::forms::FormTemplateWhereInput,
    order_by: Vec<crate::forms::FormTemplateOrderByWithRelationInput>,
    take: Option<i64>,
    skip: Option<i64>,
}

impl<'a, E> FormTemplateFindManyBuilder<'a, E> {
    pub fn order_by(
        mut self,
        order_by: &[crate::forms::FormTemplateOrderByWithRelationInput],
    ) -> Self {
        self.order_by = order_by.to_vec();
        self
    }

    pub fn take(mut self, n: i64) -> Self {
        self.take = Some(n);
        self
    }

    pub fn skip(mut self, n: i64) -> Self {
        self.skip = Some(n);
        self
    }

    pub async fn exec<'e>(self) -> sqlx::Result<Vec<crate::forms::FormTemplate>>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut qb = sqlx::QueryBuilder::new(
            r#"SELECT "id", "firm_id", "name", "description", "status", "current_version", "created_by_id", "created_at", "updated_at" FROM "form_templates" WHERE "#,
        );
        if !crate::engine::sqlx_postgres::forms::push_form_template_where(&mut qb, self.where_input)
        {
            qb.push("TRUE");
        }
        if !self.order_by.is_empty() {
            qb.push(" ORDER BY ");
            let mut first = true;
            for ob in &self.order_by {
                if let Some(o) = ob.id {
                    if !first {
                        qb.push(", ");
                    }
                    first = false;
                    qb.push("\"id\" ");
                    qb.push(crate::engine::sqlx_postgres::filters::sort_order_sql(o));
                }
                if let Some(o) = ob.firm_id {
                    if !first {
                        qb.push(", ");
                    }
                    first = false;
                    qb.push("\"firm_id\" ");
                    qb.push(crate::engine::sqlx_postgres::filters::sort_order_sql(o));
                }
                if let Some(o) = ob.name {
                    if !first {
                        qb.push(", ");
                    }
                    first = false;
                    qb.push("\"name\" ");
                    qb.push(crate::engine::sqlx_postgres::filters::sort_order_sql(o));
                }
                if let Some(o) = ob.description {
                    if !first {
                        qb.push(", ");
                    }
                    first = false;
                    qb.push("\"description\" ");
                    qb.push(crate::engine::sqlx_postgres::filters::sort_order_sql(o));
                }
                if let Some(o) = ob.status {
                    if !first {
                        qb.push(", ");
                    }
                    first = false;
                    qb.push("\"status\" ");
                    qb.push(crate::engine::sqlx_postgres::filters::sort_order_sql(o));
                }
                if let Some(o) = ob.current_version {
                    if !first {
                        qb.push(", ");
                    }
                    first = false;
                    qb.push("\"current_version\" ");
                    qb.push(crate::engine::sqlx_postgres::filters::sort_order_sql(o));
                }
                if let Some(o) = ob.created_by_id {
                    if !first {
                        qb.push(", ");
                    }
                    first = false;
                    qb.push("\"created_by_id\" ");
                    qb.push(crate::engine::sqlx_postgres::filters::sort_order_sql(o));
                }
                if let Some(o) = ob.created_at {
                    if !first {
                        qb.push(", ");
                    }
                    first = false;
                    qb.push("\"created_at\" ");
                    qb.push(crate::engine::sqlx_postgres::filters::sort_order_sql(o));
                }
                if let Some(o) = ob.updated_at {
                    if !first {
                        qb.push(", ");
                    }
                    first = false;
                    qb.push("\"updated_at\" ");
                    qb.push(crate::engine::sqlx_postgres::filters::sort_order_sql(o));
                }
                // TODO(prisma-rust-generator): sorting by relation `firm` not yet supported.
                let _ = &ob.firm;
                // TODO(prisma-rust-generator): sorting by relation `createdBy` not yet supported.
                let _ = &ob.created_by;
                // TODO(prisma-rust-generator): sorting by relation `versions` not yet supported.
                let _ = &ob.versions;
                // TODO(prisma-rust-generator): sorting by relation `distributions` not yet supported.
                let _ = &ob.distributions;
                // TODO(prisma-rust-generator): sorting by relation `submissions` not yet supported.
                let _ = &ob.submissions;
            }
        }
        if let Some(n) = self.take {
            qb.push(" LIMIT ");
            qb.push_bind(n);
        }
        if let Some(n) = self.skip {
            qb.push(" OFFSET ");
            qb.push_bind(n);
        }
        qb.build_query_as::<crate::forms::FormTemplate>()
            .fetch_all(self.executor)
            .await
    }
}

impl crate::forms::FormTemplate {
    pub fn find_many<'a, E>(
        executor: E,
        w: &'a crate::forms::FormTemplateWhereInput,
    ) -> FormTemplateFindManyBuilder<'a, E> {
        FormTemplateFindManyBuilder {
            executor,
            where_input: w,
            order_by: Vec::new(),
            take: None,
            skip: None,
        }
    }
}

impl crate::forms::FormTemplate {
    pub async fn count<'e, E>(
        executor: E,
        w: Option<&crate::forms::FormTemplateWhereInput>,
    ) -> sqlx::Result<i64>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut qb = sqlx::QueryBuilder::new(r#"SELECT COUNT(*) FROM "form_templates" WHERE "#);
        if let Some(w) = w {
            if !crate::engine::sqlx_postgres::forms::push_form_template_where(&mut qb, w) {
                qb.push("TRUE");
            }
        } else {
            qb.push("TRUE");
        }
        let row = qb.build_query_as::<(i64,)>().fetch_one(executor).await?;
        Ok(row.0)
    }
}

impl crate::forms::FormTemplate {
    pub async fn create<'e, E>(
        executor: E,
        input: crate::forms::FormTemplateUncheckedCreateInput,
    ) -> sqlx::Result<Self>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut cols = String::new();
        cols.push_str("\"firm_id\"");
        cols.push_str(", ");
        cols.push_str("\"name\"");
        cols.push_str(", ");
        cols.push_str("\"created_by_id\"");
        if input.description.is_some() {
            cols.push_str(", ");
            cols.push_str("\"description\"");
        }
        let mut qb = sqlx::QueryBuilder::new(format!(
            r#"INSERT INTO "form_templates" ({}) VALUES ("#,
            cols,
        ));
        qb.push_bind(input.firm_id);
        qb.push(", ");
        qb.push_bind(input.name);
        qb.push(", ");
        qb.push_bind(input.created_by_id);
        if let Some(v) = input.description {
            qb.push(", ");
            qb.push_bind(v);
        }
        qb.push(r#") RETURNING "id", "firm_id", "name", "description", "status", "current_version", "created_by_id", "created_at", "updated_at""#);
        qb.build_query_as::<Self>().fetch_one(executor).await
    }
}

impl crate::forms::FormTemplate {
    pub async fn create_many<'e, E>(
        executor: E,
        inputs: &[crate::forms::FormTemplateUncheckedCreateInput],
    ) -> sqlx::Result<u64>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        if inputs.is_empty() {
            return Ok(0);
        }
        let mut qb = sqlx::QueryBuilder::new(
            r#"INSERT INTO "form_templates" ("firm_id", "name", "description", "created_by_id") "#,
        );
        qb.push_values(inputs, |mut row, input| {
            row.push_bind(input.firm_id.clone());
            row.push_bind(input.name.clone());
            row.push_bind(input.description.clone());
            row.push_bind(input.created_by_id.clone());
        });
        let result = qb.build().execute(executor).await?;
        Ok(result.rows_affected())
    }
}

impl crate::forms::FormTemplate {
    pub async fn update<'e, E>(
        executor: E,
        w: &crate::forms::FormTemplateWhereUniqueInput,
        input: crate::forms::FormTemplateUncheckedUpdateInput,
    ) -> sqlx::Result<Self>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut qb = sqlx::QueryBuilder::new(r#"UPDATE "form_templates" SET "#);
        let mut first = true;
        if let Some(op) = input.id {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"id\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.firm_id {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"firm_id\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.name {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"name\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.description {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"description\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.status {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"status\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.current_version {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"current_version\" = ");
                qb.push_bind(v);
            }
            if let Some(v) = op.increment {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"current_version\" = \"current_version\" + ");
                qb.push_bind(v);
            }
            if let Some(v) = op.decrement {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"current_version\" = \"current_version\" - ");
                qb.push_bind(v);
            }
            if let Some(v) = op.multiply {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"current_version\" = \"current_version\" * ");
                qb.push_bind(v);
            }
            if let Some(v) = op.divide {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"current_version\" = \"current_version\" / ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.created_by_id {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"created_by_id\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.created_at {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"created_at\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.updated_at {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"updated_at\" = ");
                qb.push_bind(v);
            }
        }
        if first {
            // Nothing to update — fall back to a plain find_unique so the
            // caller still gets the row back (or RowNotFound if absent).
            return Self::find_unique(executor, w)
                .await?
                .ok_or(sqlx::Error::RowNotFound);
        }
        qb.push(" WHERE ");
        if let Some(value) = &w.id {
            qb.push("\"id\" = ");
            qb.push_bind(value.clone());
        } else {
            return Err(sqlx::Error::RowNotFound);
        }
        qb.push(r#" RETURNING "id", "firm_id", "name", "description", "status", "current_version", "created_by_id", "created_at", "updated_at""#);
        qb.build_query_as::<Self>().fetch_one(executor).await
    }
}

impl crate::forms::FormTemplate {
    pub async fn update_many<'e, E>(
        executor: E,
        w: &crate::forms::FormTemplateWhereInput,
        input: crate::forms::FormTemplateUncheckedUpdateManyInput,
    ) -> sqlx::Result<u64>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut qb = sqlx::QueryBuilder::new(r#"UPDATE "form_templates" SET "#);
        let mut first = true;
        if let Some(op) = input.id {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"id\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.firm_id {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"firm_id\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.name {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"name\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.description {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"description\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.status {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"status\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.current_version {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"current_version\" = ");
                qb.push_bind(v);
            }
            if let Some(v) = op.increment {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"current_version\" = \"current_version\" + ");
                qb.push_bind(v);
            }
            if let Some(v) = op.decrement {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"current_version\" = \"current_version\" - ");
                qb.push_bind(v);
            }
            if let Some(v) = op.multiply {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"current_version\" = \"current_version\" * ");
                qb.push_bind(v);
            }
            if let Some(v) = op.divide {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"current_version\" = \"current_version\" / ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.created_by_id {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"created_by_id\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.created_at {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"created_at\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.updated_at {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"updated_at\" = ");
                qb.push_bind(v);
            }
        }
        if first {
            // No-op update — return 0 rows affected without issuing SQL.
            return Ok(0);
        }
        qb.push(" WHERE ");
        if !crate::engine::sqlx_postgres::forms::push_form_template_where(&mut qb, w) {
            qb.push("TRUE");
        }
        let result = qb.build().execute(executor).await?;
        Ok(result.rows_affected())
    }
}

impl crate::forms::FormTemplate {
    pub async fn delete<'e, E>(
        executor: E,
        w: &crate::forms::FormTemplateWhereUniqueInput,
    ) -> sqlx::Result<Self>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut qb = sqlx::QueryBuilder::new(r#"DELETE FROM "form_templates" WHERE "#);
        if let Some(value) = &w.id {
            qb.push("\"id\" = ");
            qb.push_bind(value.clone());
        } else {
            return Err(sqlx::Error::RowNotFound);
        }
        qb.push(r#" RETURNING "id", "firm_id", "name", "description", "status", "current_version", "created_by_id", "created_at", "updated_at""#);
        qb.build_query_as::<Self>().fetch_one(executor).await
    }
}

impl crate::forms::FormTemplate {
    pub async fn delete_many<'e, E>(
        executor: E,
        w: &crate::forms::FormTemplateWhereInput,
    ) -> sqlx::Result<u64>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut qb = sqlx::QueryBuilder::new(r#"DELETE FROM "form_templates" WHERE "#);
        if !crate::engine::sqlx_postgres::forms::push_form_template_where(&mut qb, w) {
            qb.push("TRUE");
        }
        let result = qb.build().execute(executor).await?;
        Ok(result.rows_affected())
    }
}

#[derive(Debug, Clone, Default, PartialEq, Eq, Hash, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct FormTemplateCountAggregateResult {
    pub id: Option<i64>,
    pub firm_id: Option<i64>,
    pub name: Option<i64>,
    pub description: Option<i64>,
    pub status: Option<i64>,
    pub current_version: Option<i64>,
    pub created_by_id: Option<i64>,
    pub created_at: Option<i64>,
    pub updated_at: Option<i64>,
    #[serde(rename = "_all")]
    pub aggregate_all: Option<i64>,
}

#[derive(Debug, Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct FormTemplateAvgAggregateResult {
    pub current_version: Option<f64>,
}

#[derive(Debug, Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct FormTemplateSumAggregateResult {
    pub current_version: Option<f64>,
}

#[derive(Debug, Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct FormTemplateMinAggregateResult {
    pub id: Option<String>,
    pub firm_id: Option<String>,
    pub name: Option<String>,
    pub description: Option<String>,
    pub current_version: Option<i32>,
    pub created_by_id: Option<String>,
    pub created_at: Option<chrono::DateTime<chrono::Utc>>,
    pub updated_at: Option<chrono::DateTime<chrono::Utc>>,
}

#[derive(Debug, Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct FormTemplateMaxAggregateResult {
    pub id: Option<String>,
    pub firm_id: Option<String>,
    pub name: Option<String>,
    pub description: Option<String>,
    pub current_version: Option<i32>,
    pub created_by_id: Option<String>,
    pub created_at: Option<chrono::DateTime<chrono::Utc>>,
    pub updated_at: Option<chrono::DateTime<chrono::Utc>>,
}

#[derive(Debug, Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct FormTemplateAggregateResult {
    #[serde(rename = "_count")]
    pub aggregate_count: Option<FormTemplateCountAggregateResult>,
    #[serde(rename = "_avg")]
    pub aggregate_avg: Option<FormTemplateAvgAggregateResult>,
    #[serde(rename = "_sum")]
    pub aggregate_sum: Option<FormTemplateSumAggregateResult>,
    #[serde(rename = "_min")]
    pub aggregate_min: Option<FormTemplateMinAggregateResult>,
    #[serde(rename = "_max")]
    pub aggregate_max: Option<FormTemplateMaxAggregateResult>,
}

impl crate::forms::FormTemplate {
    pub async fn aggregate<'e, E>(
        executor: E,
        input: &crate::forms::FormTemplateAggregateInput,
    ) -> sqlx::Result<FormTemplateAggregateResult>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut qb = sqlx::QueryBuilder::new(
            r#"SELECT COUNT(*) AS count_all, COUNT("id") AS count_id, COUNT("firm_id") AS count_firm_id, COUNT("name") AS count_name, COUNT("description") AS count_description, COUNT("status") AS count_status, COUNT("current_version") AS count_current_version, COUNT("created_by_id") AS count_created_by_id, COUNT("created_at") AS count_created_at, COUNT("updated_at") AS count_updated_at, AVG("current_version")::double precision AS avg_current_version, SUM("current_version")::double precision AS sum_current_version, MIN("id") AS min_id, MAX("id") AS max_id, MIN("firm_id") AS min_firm_id, MAX("firm_id") AS max_firm_id, MIN("name") AS min_name, MAX("name") AS max_name, MIN("description") AS min_description, MAX("description") AS max_description, MIN("current_version") AS min_current_version, MAX("current_version") AS max_current_version, MIN("created_by_id") AS min_created_by_id, MAX("created_by_id") AS max_created_by_id, MIN("created_at") AS min_created_at, MAX("created_at") AS max_created_at, MIN("updated_at") AS min_updated_at, MAX("updated_at") AS max_updated_at FROM "form_templates" WHERE "#,
        );
        if let Some(w) = &input.r#where {
            if !crate::engine::sqlx_postgres::forms::push_form_template_where(&mut qb, w) {
                qb.push("TRUE");
            }
        } else {
            qb.push("TRUE");
        }
        let row = qb.build().fetch_one(executor).await?;
        use sqlx::Row;
        Ok(FormTemplateAggregateResult {
            aggregate_count: input
                .aggregate_count
                .as_ref()
                .map(|req| -> sqlx::Result<_> {
                    Ok(FormTemplateCountAggregateResult {
                        id: if req.id.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_id")?
                        } else {
                            None
                        },
                        firm_id: if req.firm_id.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_firm_id")?
                        } else {
                            None
                        },
                        name: if req.name.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_name")?
                        } else {
                            None
                        },
                        description: if req.description.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_description")?
                        } else {
                            None
                        },
                        status: if req.status.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_status")?
                        } else {
                            None
                        },
                        current_version: if req.current_version.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_current_version")?
                        } else {
                            None
                        },
                        created_by_id: if req.created_by_id.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_created_by_id")?
                        } else {
                            None
                        },
                        created_at: if req.created_at.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_created_at")?
                        } else {
                            None
                        },
                        updated_at: if req.updated_at.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_updated_at")?
                        } else {
                            None
                        },
                        aggregate_all: if req.aggregate_all.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_all")?
                        } else {
                            None
                        },
                    })
                })
                .transpose()?,
            aggregate_avg: input
                .aggregate_avg
                .as_ref()
                .map(|req| -> sqlx::Result<_> {
                    Ok(FormTemplateAvgAggregateResult {
                        current_version: if req.current_version.unwrap_or(false) {
                            row.try_get::<Option<f64>, _>("avg_current_version")?
                        } else {
                            None
                        },
                    })
                })
                .transpose()?,
            aggregate_sum: input
                .aggregate_sum
                .as_ref()
                .map(|req| -> sqlx::Result<_> {
                    Ok(FormTemplateSumAggregateResult {
                        current_version: if req.current_version.unwrap_or(false) {
                            row.try_get::<Option<f64>, _>("sum_current_version")?
                        } else {
                            None
                        },
                    })
                })
                .transpose()?,
            aggregate_min: input
                .aggregate_min
                .as_ref()
                .map(|req| -> sqlx::Result<_> {
                    Ok(FormTemplateMinAggregateResult {
                        id: if req.id.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("min_id")?
                        } else {
                            None
                        },
                        firm_id: if req.firm_id.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("min_firm_id")?
                        } else {
                            None
                        },
                        name: if req.name.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("min_name")?
                        } else {
                            None
                        },
                        description: if req.description.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("min_description")?
                        } else {
                            None
                        },
                        current_version: if req.current_version.unwrap_or(false) {
                            row.try_get::<Option<i32>, _>("min_current_version")?
                        } else {
                            None
                        },
                        created_by_id: if req.created_by_id.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("min_created_by_id")?
                        } else {
                            None
                        },
                        created_at: if req.created_at.unwrap_or(false) {
                            row.try_get::<Option<chrono::DateTime<chrono::Utc>>, _>(
                                "min_created_at",
                            )?
                        } else {
                            None
                        },
                        updated_at: if req.updated_at.unwrap_or(false) {
                            row.try_get::<Option<chrono::DateTime<chrono::Utc>>, _>(
                                "min_updated_at",
                            )?
                        } else {
                            None
                        },
                    })
                })
                .transpose()?,
            aggregate_max: input
                .aggregate_max
                .as_ref()
                .map(|req| -> sqlx::Result<_> {
                    Ok(FormTemplateMaxAggregateResult {
                        id: if req.id.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("max_id")?
                        } else {
                            None
                        },
                        firm_id: if req.firm_id.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("max_firm_id")?
                        } else {
                            None
                        },
                        name: if req.name.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("max_name")?
                        } else {
                            None
                        },
                        description: if req.description.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("max_description")?
                        } else {
                            None
                        },
                        current_version: if req.current_version.unwrap_or(false) {
                            row.try_get::<Option<i32>, _>("max_current_version")?
                        } else {
                            None
                        },
                        created_by_id: if req.created_by_id.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("max_created_by_id")?
                        } else {
                            None
                        },
                        created_at: if req.created_at.unwrap_or(false) {
                            row.try_get::<Option<chrono::DateTime<chrono::Utc>>, _>(
                                "max_created_at",
                            )?
                        } else {
                            None
                        },
                        updated_at: if req.updated_at.unwrap_or(false) {
                            row.try_get::<Option<chrono::DateTime<chrono::Utc>>, _>(
                                "max_updated_at",
                            )?
                        } else {
                            None
                        },
                    })
                })
                .transpose()?,
        })
    }
}

impl sqlx::FromRow<'_, sqlx::postgres::PgRow> for crate::forms::FormTemplateVersion {
    fn from_row(row: &sqlx::postgres::PgRow) -> sqlx::Result<Self> {
        Ok(crate::forms::FormTemplateVersion {
            id: row.try_get("id")?,
            template_id: row.try_get("template_id")?,
            version_number: row.try_get("version_number")?,
            schema: row.try_get("schema")?,
            created_at: row.try_get("created_at")?,
            template: None,
            distributions: Vec::new(),
            submissions: Vec::new(),
        })
    }
}

pub(crate) fn push_form_template_version_where(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    w: &crate::forms::FormTemplateVersionWhereInput,
) -> bool {
    let mut any = false;
    if let Some(f) = &w.id {
        if any {
            qb.push(" AND ");
        }
        any = true;
        crate::engine::sqlx_postgres::filters::push_string_filter(qb, "id", f);
    }
    if let Some(f) = &w.template_id {
        if any {
            qb.push(" AND ");
        }
        any = true;
        crate::engine::sqlx_postgres::filters::push_string_filter(qb, "template_id", f);
    }
    if let Some(f) = &w.version_number {
        if any {
            qb.push(" AND ");
        }
        any = true;
        crate::engine::sqlx_postgres::filters::push_int_filter(qb, "version_number", f);
    }
    if let Some(f) = &w.schema {
        if any {
            qb.push(" AND ");
        }
        any = true;
        crate::engine::sqlx_postgres::filters::push_json_filter(qb, "schema", f);
    }
    if let Some(f) = &w.created_at {
        if any {
            qb.push(" AND ");
        }
        any = true;
        crate::engine::sqlx_postgres::filters::push_datetime_filter(qb, "created_at", f);
    }
    if let Some(rel) = &w.template {
        if let Some(sub) = &rel.is {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("EXISTS (SELECT 1 FROM \"form_templates\" t WHERE t.\"id\" = \"form_template_versions\".\"template_id\" AND (");
            if !crate::engine::sqlx_postgres::forms::push_form_template_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
        if let Some(sub) = &rel.is_not {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("NOT EXISTS (SELECT 1 FROM \"form_templates\" t WHERE t.\"id\" = \"form_template_versions\".\"template_id\" AND (");
            if !crate::engine::sqlx_postgres::forms::push_form_template_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
    }
    if let Some(rel) = &w.distributions {
        if let Some(sub) = &rel.every {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("NOT EXISTS (SELECT 1 FROM \"form_distributions\" t WHERE t.\"template_version_id\" = \"form_template_versions\".\"id\" AND NOT (");
            if !crate::engine::sqlx_postgres::forms::push_form_distribution_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
        if let Some(sub) = &rel.some {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("EXISTS (SELECT 1 FROM \"form_distributions\" t WHERE t.\"template_version_id\" = \"form_template_versions\".\"id\" AND (");
            if !crate::engine::sqlx_postgres::forms::push_form_distribution_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
        if let Some(sub) = &rel.none {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("NOT EXISTS (SELECT 1 FROM \"form_distributions\" t WHERE t.\"template_version_id\" = \"form_template_versions\".\"id\" AND (");
            if !crate::engine::sqlx_postgres::forms::push_form_distribution_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
    }
    if let Some(rel) = &w.submissions {
        if let Some(sub) = &rel.every {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("NOT EXISTS (SELECT 1 FROM \"form_submissions\" t WHERE t.\"template_version_id\" = \"form_template_versions\".\"id\" AND NOT (");
            if !crate::engine::sqlx_postgres::forms::push_form_submission_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
        if let Some(sub) = &rel.some {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("EXISTS (SELECT 1 FROM \"form_submissions\" t WHERE t.\"template_version_id\" = \"form_template_versions\".\"id\" AND (");
            if !crate::engine::sqlx_postgres::forms::push_form_submission_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
        if let Some(sub) = &rel.none {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("NOT EXISTS (SELECT 1 FROM \"form_submissions\" t WHERE t.\"template_version_id\" = \"form_template_versions\".\"id\" AND (");
            if !crate::engine::sqlx_postgres::forms::push_form_submission_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
    }
    if let Some(group) = &w.and {
        for sub in group {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("(");
            if !push_form_template_version_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push(")");
        }
    }
    if let Some(group) = &w.or {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("(");
        let mut first = true;
        for sub in group {
            if !first {
                qb.push(" OR ");
            }
            first = false;
            qb.push("(");
            if !push_form_template_version_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push(")");
        }
        qb.push(")");
    }
    if let Some(group) = &w.not {
        for sub in group {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("NOT (");
            if !push_form_template_version_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push(")");
        }
    }
    any
}

impl crate::forms::FormTemplateVersion {
    pub async fn find_unique<'e, E>(
        executor: E,
        w: &crate::forms::FormTemplateVersionWhereUniqueInput,
    ) -> sqlx::Result<Option<Self>>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let cols = "\"id\", \"template_id\", \"version_number\", \"schema\", \"created_at\"";
        let mut qb = sqlx::QueryBuilder::new(format!(
            r#"SELECT {} FROM "form_template_versions" WHERE "#,
            cols,
        ));
        // For v1, support only the @id field. Composite uniques are out of scope.
        // The WhereUniqueInput has Option<T> per @id/@unique field; we pick the
        // first Some(). Multi-Some inputs cover composite uniques — TODO Task v2.
        if let Some(value) = &w.id {
            qb.push("\"id\" = ");
            qb.push_bind(value.clone());
        } else {
            return Ok(None);
        }
        qb.build_query_as::<Self>().fetch_optional(executor).await
    }
}

impl crate::forms::FormTemplateVersion {
    pub async fn find_first<'e, E>(
        executor: E,
        w: &crate::forms::FormTemplateVersionWhereInput,
    ) -> sqlx::Result<Option<Self>>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut qb = sqlx::QueryBuilder::new(
            r#"SELECT "id", "template_id", "version_number", "schema", "created_at" FROM "form_template_versions" WHERE "#,
        );
        if !crate::engine::sqlx_postgres::forms::push_form_template_version_where(&mut qb, w) {
            qb.push("TRUE");
        }
        qb.push(" LIMIT 1");
        qb.build_query_as::<Self>().fetch_optional(executor).await
    }
}

pub struct FormTemplateVersionFindManyBuilder<'a, E> {
    executor: E,
    where_input: &'a crate::forms::FormTemplateVersionWhereInput,
    order_by: Vec<crate::forms::FormTemplateVersionOrderByWithRelationInput>,
    take: Option<i64>,
    skip: Option<i64>,
}

impl<'a, E> FormTemplateVersionFindManyBuilder<'a, E> {
    pub fn order_by(
        mut self,
        order_by: &[crate::forms::FormTemplateVersionOrderByWithRelationInput],
    ) -> Self {
        self.order_by = order_by.to_vec();
        self
    }

    pub fn take(mut self, n: i64) -> Self {
        self.take = Some(n);
        self
    }

    pub fn skip(mut self, n: i64) -> Self {
        self.skip = Some(n);
        self
    }

    pub async fn exec<'e>(self) -> sqlx::Result<Vec<crate::forms::FormTemplateVersion>>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut qb = sqlx::QueryBuilder::new(
            r#"SELECT "id", "template_id", "version_number", "schema", "created_at" FROM "form_template_versions" WHERE "#,
        );
        if !crate::engine::sqlx_postgres::forms::push_form_template_version_where(
            &mut qb,
            self.where_input,
        ) {
            qb.push("TRUE");
        }
        if !self.order_by.is_empty() {
            qb.push(" ORDER BY ");
            let mut first = true;
            for ob in &self.order_by {
                if let Some(o) = ob.id {
                    if !first {
                        qb.push(", ");
                    }
                    first = false;
                    qb.push("\"id\" ");
                    qb.push(crate::engine::sqlx_postgres::filters::sort_order_sql(o));
                }
                if let Some(o) = ob.template_id {
                    if !first {
                        qb.push(", ");
                    }
                    first = false;
                    qb.push("\"template_id\" ");
                    qb.push(crate::engine::sqlx_postgres::filters::sort_order_sql(o));
                }
                if let Some(o) = ob.version_number {
                    if !first {
                        qb.push(", ");
                    }
                    first = false;
                    qb.push("\"version_number\" ");
                    qb.push(crate::engine::sqlx_postgres::filters::sort_order_sql(o));
                }
                if let Some(o) = ob.schema {
                    if !first {
                        qb.push(", ");
                    }
                    first = false;
                    qb.push("\"schema\" ");
                    qb.push(crate::engine::sqlx_postgres::filters::sort_order_sql(o));
                }
                if let Some(o) = ob.created_at {
                    if !first {
                        qb.push(", ");
                    }
                    first = false;
                    qb.push("\"created_at\" ");
                    qb.push(crate::engine::sqlx_postgres::filters::sort_order_sql(o));
                }
                // TODO(prisma-rust-generator): sorting by relation `template` not yet supported.
                let _ = &ob.template;
                // TODO(prisma-rust-generator): sorting by relation `distributions` not yet supported.
                let _ = &ob.distributions;
                // TODO(prisma-rust-generator): sorting by relation `submissions` not yet supported.
                let _ = &ob.submissions;
            }
        }
        if let Some(n) = self.take {
            qb.push(" LIMIT ");
            qb.push_bind(n);
        }
        if let Some(n) = self.skip {
            qb.push(" OFFSET ");
            qb.push_bind(n);
        }
        qb.build_query_as::<crate::forms::FormTemplateVersion>()
            .fetch_all(self.executor)
            .await
    }
}

impl crate::forms::FormTemplateVersion {
    pub fn find_many<'a, E>(
        executor: E,
        w: &'a crate::forms::FormTemplateVersionWhereInput,
    ) -> FormTemplateVersionFindManyBuilder<'a, E> {
        FormTemplateVersionFindManyBuilder {
            executor,
            where_input: w,
            order_by: Vec::new(),
            take: None,
            skip: None,
        }
    }
}

impl crate::forms::FormTemplateVersion {
    pub async fn count<'e, E>(
        executor: E,
        w: Option<&crate::forms::FormTemplateVersionWhereInput>,
    ) -> sqlx::Result<i64>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut qb =
            sqlx::QueryBuilder::new(r#"SELECT COUNT(*) FROM "form_template_versions" WHERE "#);
        if let Some(w) = w {
            if !crate::engine::sqlx_postgres::forms::push_form_template_version_where(&mut qb, w) {
                qb.push("TRUE");
            }
        } else {
            qb.push("TRUE");
        }
        let row = qb.build_query_as::<(i64,)>().fetch_one(executor).await?;
        Ok(row.0)
    }
}

impl crate::forms::FormTemplateVersion {
    pub async fn create<'e, E>(
        executor: E,
        input: crate::forms::FormTemplateVersionUncheckedCreateInput,
    ) -> sqlx::Result<Self>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut cols = String::new();
        cols.push_str("\"template_id\"");
        cols.push_str(", ");
        cols.push_str("\"version_number\"");
        cols.push_str(", ");
        cols.push_str("\"schema\"");
        let mut qb = sqlx::QueryBuilder::new(format!(
            r#"INSERT INTO "form_template_versions" ({}) VALUES ("#,
            cols,
        ));
        qb.push_bind(input.template_id);
        qb.push(", ");
        qb.push_bind(input.version_number);
        qb.push(", ");
        qb.push_bind(input.schema);
        qb.push(r#") RETURNING "id", "template_id", "version_number", "schema", "created_at""#);
        qb.build_query_as::<Self>().fetch_one(executor).await
    }
}

impl crate::forms::FormTemplateVersion {
    pub async fn create_many<'e, E>(
        executor: E,
        inputs: &[crate::forms::FormTemplateVersionUncheckedCreateInput],
    ) -> sqlx::Result<u64>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        if inputs.is_empty() {
            return Ok(0);
        }
        let mut qb = sqlx::QueryBuilder::new(
            r#"INSERT INTO "form_template_versions" ("template_id", "version_number", "schema") "#,
        );
        qb.push_values(inputs, |mut row, input| {
            row.push_bind(input.template_id.clone());
            row.push_bind(input.version_number);
            row.push_bind(input.schema.clone());
        });
        let result = qb.build().execute(executor).await?;
        Ok(result.rows_affected())
    }
}

impl crate::forms::FormTemplateVersion {
    pub async fn update<'e, E>(
        executor: E,
        w: &crate::forms::FormTemplateVersionWhereUniqueInput,
        input: crate::forms::FormTemplateVersionUncheckedUpdateInput,
    ) -> sqlx::Result<Self>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut qb = sqlx::QueryBuilder::new(r#"UPDATE "form_template_versions" SET "#);
        let mut first = true;
        if let Some(op) = input.id {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"id\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.template_id {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"template_id\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.version_number {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"version_number\" = ");
                qb.push_bind(v);
            }
            if let Some(v) = op.increment {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"version_number\" = \"version_number\" + ");
                qb.push_bind(v);
            }
            if let Some(v) = op.decrement {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"version_number\" = \"version_number\" - ");
                qb.push_bind(v);
            }
            if let Some(v) = op.multiply {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"version_number\" = \"version_number\" * ");
                qb.push_bind(v);
            }
            if let Some(v) = op.divide {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"version_number\" = \"version_number\" / ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.schema {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"schema\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.created_at {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"created_at\" = ");
                qb.push_bind(v);
            }
        }
        if first {
            // Nothing to update — fall back to a plain find_unique so the
            // caller still gets the row back (or RowNotFound if absent).
            return Self::find_unique(executor, w)
                .await?
                .ok_or(sqlx::Error::RowNotFound);
        }
        qb.push(" WHERE ");
        if let Some(value) = &w.id {
            qb.push("\"id\" = ");
            qb.push_bind(value.clone());
        } else {
            return Err(sqlx::Error::RowNotFound);
        }
        qb.push(r#" RETURNING "id", "template_id", "version_number", "schema", "created_at""#);
        qb.build_query_as::<Self>().fetch_one(executor).await
    }
}

impl crate::forms::FormTemplateVersion {
    pub async fn update_many<'e, E>(
        executor: E,
        w: &crate::forms::FormTemplateVersionWhereInput,
        input: crate::forms::FormTemplateVersionUncheckedUpdateManyInput,
    ) -> sqlx::Result<u64>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut qb = sqlx::QueryBuilder::new(r#"UPDATE "form_template_versions" SET "#);
        let mut first = true;
        if let Some(op) = input.id {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"id\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.template_id {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"template_id\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.version_number {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"version_number\" = ");
                qb.push_bind(v);
            }
            if let Some(v) = op.increment {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"version_number\" = \"version_number\" + ");
                qb.push_bind(v);
            }
            if let Some(v) = op.decrement {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"version_number\" = \"version_number\" - ");
                qb.push_bind(v);
            }
            if let Some(v) = op.multiply {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"version_number\" = \"version_number\" * ");
                qb.push_bind(v);
            }
            if let Some(v) = op.divide {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"version_number\" = \"version_number\" / ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.schema {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"schema\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.created_at {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"created_at\" = ");
                qb.push_bind(v);
            }
        }
        if first {
            // No-op update — return 0 rows affected without issuing SQL.
            return Ok(0);
        }
        qb.push(" WHERE ");
        if !crate::engine::sqlx_postgres::forms::push_form_template_version_where(&mut qb, w) {
            qb.push("TRUE");
        }
        let result = qb.build().execute(executor).await?;
        Ok(result.rows_affected())
    }
}

impl crate::forms::FormTemplateVersion {
    pub async fn delete<'e, E>(
        executor: E,
        w: &crate::forms::FormTemplateVersionWhereUniqueInput,
    ) -> sqlx::Result<Self>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut qb = sqlx::QueryBuilder::new(r#"DELETE FROM "form_template_versions" WHERE "#);
        if let Some(value) = &w.id {
            qb.push("\"id\" = ");
            qb.push_bind(value.clone());
        } else {
            return Err(sqlx::Error::RowNotFound);
        }
        qb.push(r#" RETURNING "id", "template_id", "version_number", "schema", "created_at""#);
        qb.build_query_as::<Self>().fetch_one(executor).await
    }
}

impl crate::forms::FormTemplateVersion {
    pub async fn delete_many<'e, E>(
        executor: E,
        w: &crate::forms::FormTemplateVersionWhereInput,
    ) -> sqlx::Result<u64>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut qb = sqlx::QueryBuilder::new(r#"DELETE FROM "form_template_versions" WHERE "#);
        if !crate::engine::sqlx_postgres::forms::push_form_template_version_where(&mut qb, w) {
            qb.push("TRUE");
        }
        let result = qb.build().execute(executor).await?;
        Ok(result.rows_affected())
    }
}

#[derive(Debug, Clone, Default, PartialEq, Eq, Hash, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct FormTemplateVersionCountAggregateResult {
    pub id: Option<i64>,
    pub template_id: Option<i64>,
    pub version_number: Option<i64>,
    pub schema: Option<i64>,
    pub created_at: Option<i64>,
    #[serde(rename = "_all")]
    pub aggregate_all: Option<i64>,
}

#[derive(Debug, Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct FormTemplateVersionAvgAggregateResult {
    pub version_number: Option<f64>,
}

#[derive(Debug, Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct FormTemplateVersionSumAggregateResult {
    pub version_number: Option<f64>,
}

#[derive(Debug, Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct FormTemplateVersionMinAggregateResult {
    pub id: Option<String>,
    pub template_id: Option<String>,
    pub version_number: Option<i32>,
    pub created_at: Option<chrono::DateTime<chrono::Utc>>,
}

#[derive(Debug, Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct FormTemplateVersionMaxAggregateResult {
    pub id: Option<String>,
    pub template_id: Option<String>,
    pub version_number: Option<i32>,
    pub created_at: Option<chrono::DateTime<chrono::Utc>>,
}

#[derive(Debug, Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct FormTemplateVersionAggregateResult {
    #[serde(rename = "_count")]
    pub aggregate_count: Option<FormTemplateVersionCountAggregateResult>,
    #[serde(rename = "_avg")]
    pub aggregate_avg: Option<FormTemplateVersionAvgAggregateResult>,
    #[serde(rename = "_sum")]
    pub aggregate_sum: Option<FormTemplateVersionSumAggregateResult>,
    #[serde(rename = "_min")]
    pub aggregate_min: Option<FormTemplateVersionMinAggregateResult>,
    #[serde(rename = "_max")]
    pub aggregate_max: Option<FormTemplateVersionMaxAggregateResult>,
}

impl crate::forms::FormTemplateVersion {
    pub async fn aggregate<'e, E>(
        executor: E,
        input: &crate::forms::FormTemplateVersionAggregateInput,
    ) -> sqlx::Result<FormTemplateVersionAggregateResult>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut qb = sqlx::QueryBuilder::new(
            r#"SELECT COUNT(*) AS count_all, COUNT("id") AS count_id, COUNT("template_id") AS count_template_id, COUNT("version_number") AS count_version_number, COUNT("schema") AS count_schema, COUNT("created_at") AS count_created_at, AVG("version_number")::double precision AS avg_version_number, SUM("version_number")::double precision AS sum_version_number, MIN("id") AS min_id, MAX("id") AS max_id, MIN("template_id") AS min_template_id, MAX("template_id") AS max_template_id, MIN("version_number") AS min_version_number, MAX("version_number") AS max_version_number, MIN("created_at") AS min_created_at, MAX("created_at") AS max_created_at FROM "form_template_versions" WHERE "#,
        );
        if let Some(w) = &input.r#where {
            if !crate::engine::sqlx_postgres::forms::push_form_template_version_where(&mut qb, w) {
                qb.push("TRUE");
            }
        } else {
            qb.push("TRUE");
        }
        let row = qb.build().fetch_one(executor).await?;
        use sqlx::Row;
        Ok(FormTemplateVersionAggregateResult {
            aggregate_count: input
                .aggregate_count
                .as_ref()
                .map(|req| -> sqlx::Result<_> {
                    Ok(FormTemplateVersionCountAggregateResult {
                        id: if req.id.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_id")?
                        } else {
                            None
                        },
                        template_id: if req.template_id.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_template_id")?
                        } else {
                            None
                        },
                        version_number: if req.version_number.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_version_number")?
                        } else {
                            None
                        },
                        schema: if req.schema.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_schema")?
                        } else {
                            None
                        },
                        created_at: if req.created_at.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_created_at")?
                        } else {
                            None
                        },
                        aggregate_all: if req.aggregate_all.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_all")?
                        } else {
                            None
                        },
                    })
                })
                .transpose()?,
            aggregate_avg: input
                .aggregate_avg
                .as_ref()
                .map(|req| -> sqlx::Result<_> {
                    Ok(FormTemplateVersionAvgAggregateResult {
                        version_number: if req.version_number.unwrap_or(false) {
                            row.try_get::<Option<f64>, _>("avg_version_number")?
                        } else {
                            None
                        },
                    })
                })
                .transpose()?,
            aggregate_sum: input
                .aggregate_sum
                .as_ref()
                .map(|req| -> sqlx::Result<_> {
                    Ok(FormTemplateVersionSumAggregateResult {
                        version_number: if req.version_number.unwrap_or(false) {
                            row.try_get::<Option<f64>, _>("sum_version_number")?
                        } else {
                            None
                        },
                    })
                })
                .transpose()?,
            aggregate_min: input
                .aggregate_min
                .as_ref()
                .map(|req| -> sqlx::Result<_> {
                    Ok(FormTemplateVersionMinAggregateResult {
                        id: if req.id.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("min_id")?
                        } else {
                            None
                        },
                        template_id: if req.template_id.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("min_template_id")?
                        } else {
                            None
                        },
                        version_number: if req.version_number.unwrap_or(false) {
                            row.try_get::<Option<i32>, _>("min_version_number")?
                        } else {
                            None
                        },
                        created_at: if req.created_at.unwrap_or(false) {
                            row.try_get::<Option<chrono::DateTime<chrono::Utc>>, _>(
                                "min_created_at",
                            )?
                        } else {
                            None
                        },
                    })
                })
                .transpose()?,
            aggregate_max: input
                .aggregate_max
                .as_ref()
                .map(|req| -> sqlx::Result<_> {
                    Ok(FormTemplateVersionMaxAggregateResult {
                        id: if req.id.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("max_id")?
                        } else {
                            None
                        },
                        template_id: if req.template_id.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("max_template_id")?
                        } else {
                            None
                        },
                        version_number: if req.version_number.unwrap_or(false) {
                            row.try_get::<Option<i32>, _>("max_version_number")?
                        } else {
                            None
                        },
                        created_at: if req.created_at.unwrap_or(false) {
                            row.try_get::<Option<chrono::DateTime<chrono::Utc>>, _>(
                                "max_created_at",
                            )?
                        } else {
                            None
                        },
                    })
                })
                .transpose()?,
        })
    }
}

impl sqlx::FromRow<'_, sqlx::postgres::PgRow> for crate::forms::FormDistribution {
    fn from_row(row: &sqlx::postgres::PgRow) -> sqlx::Result<Self> {
        Ok(crate::forms::FormDistribution {
            id: row.try_get("id")?,
            template_id: row.try_get("template_id")?,
            template_version_id: row.try_get("template_version_id")?,
            firm_id: row.try_get("firm_id")?,
            client_email: row.try_get("client_email")?,
            client_name: row.try_get("client_name")?,
            token: row.try_get("token")?,
            expires_at: row.try_get("expires_at")?,
            status: row.try_get("status")?,
            single_use: row.try_get("single_use")?,
            created_by_id: row.try_get("created_by_id")?,
            created_at: row.try_get("created_at")?,
            submitted_at: row.try_get("submitted_at")?,
            template: None,
            template_version: None,
            firm: None,
            created_by: None,
            submissions: Vec::new(),
        })
    }
}

pub(crate) fn push_form_distribution_where(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    w: &crate::forms::FormDistributionWhereInput,
) -> bool {
    let mut any = false;
    if let Some(f) = &w.id {
        if any {
            qb.push(" AND ");
        }
        any = true;
        crate::engine::sqlx_postgres::filters::push_string_filter(qb, "id", f);
    }
    if let Some(f) = &w.template_id {
        if any {
            qb.push(" AND ");
        }
        any = true;
        crate::engine::sqlx_postgres::filters::push_string_filter(qb, "template_id", f);
    }
    if let Some(f) = &w.template_version_id {
        if any {
            qb.push(" AND ");
        }
        any = true;
        crate::engine::sqlx_postgres::filters::push_string_filter(qb, "template_version_id", f);
    }
    if let Some(f) = &w.firm_id {
        if any {
            qb.push(" AND ");
        }
        any = true;
        crate::engine::sqlx_postgres::filters::push_string_filter(qb, "firm_id", f);
    }
    if let Some(f) = &w.client_email {
        if any {
            qb.push(" AND ");
        }
        any = true;
        crate::engine::sqlx_postgres::filters::push_string_filter(qb, "client_email", f);
    }
    if let Some(f) = &w.client_name {
        if any {
            qb.push(" AND ");
        }
        any = true;
        crate::engine::sqlx_postgres::filters::push_string_filter(qb, "client_name", f);
    }
    if let Some(f) = &w.token {
        if any {
            qb.push(" AND ");
        }
        any = true;
        crate::engine::sqlx_postgres::filters::push_string_filter(qb, "token", f);
    }
    if let Some(f) = &w.expires_at {
        if any {
            qb.push(" AND ");
        }
        any = true;
        crate::engine::sqlx_postgres::filters::push_datetime_filter(qb, "expires_at", f);
    }
    if let Some(f) = &w.status {
        if any {
            qb.push(" AND ");
        }
        any = true;
        crate::engine::sqlx_postgres::filters::push_distribution_status_filter(qb, "status", f);
    }
    if let Some(f) = &w.single_use {
        if any {
            qb.push(" AND ");
        }
        any = true;
        crate::engine::sqlx_postgres::filters::push_bool_filter(qb, "single_use", f);
    }
    if let Some(f) = &w.created_by_id {
        if any {
            qb.push(" AND ");
        }
        any = true;
        crate::engine::sqlx_postgres::filters::push_string_filter(qb, "created_by_id", f);
    }
    if let Some(f) = &w.created_at {
        if any {
            qb.push(" AND ");
        }
        any = true;
        crate::engine::sqlx_postgres::filters::push_datetime_filter(qb, "created_at", f);
    }
    if let Some(f) = &w.submitted_at {
        if any {
            qb.push(" AND ");
        }
        any = true;
        crate::engine::sqlx_postgres::filters::push_datetime_nullable_filter(qb, "submitted_at", f);
    }
    if let Some(rel) = &w.template {
        if let Some(sub) = &rel.is {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("EXISTS (SELECT 1 FROM \"form_templates\" t WHERE t.\"id\" = \"form_distributions\".\"template_id\" AND (");
            if !crate::engine::sqlx_postgres::forms::push_form_template_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
        if let Some(sub) = &rel.is_not {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("NOT EXISTS (SELECT 1 FROM \"form_templates\" t WHERE t.\"id\" = \"form_distributions\".\"template_id\" AND (");
            if !crate::engine::sqlx_postgres::forms::push_form_template_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
    }
    if let Some(rel) = &w.template_version {
        if let Some(sub) = &rel.is {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("EXISTS (SELECT 1 FROM \"form_template_versions\" t WHERE t.\"id\" = \"form_distributions\".\"template_version_id\" AND (");
            if !crate::engine::sqlx_postgres::forms::push_form_template_version_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
        if let Some(sub) = &rel.is_not {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("NOT EXISTS (SELECT 1 FROM \"form_template_versions\" t WHERE t.\"id\" = \"form_distributions\".\"template_version_id\" AND (");
            if !crate::engine::sqlx_postgres::forms::push_form_template_version_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
    }
    if let Some(rel) = &w.firm {
        if let Some(sub) = &rel.is {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("EXISTS (SELECT 1 FROM \"firms\" t WHERE t.\"id\" = \"form_distributions\".\"firm_id\" AND (");
            if !crate::engine::sqlx_postgres::firms::push_firm_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
        if let Some(sub) = &rel.is_not {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("NOT EXISTS (SELECT 1 FROM \"firms\" t WHERE t.\"id\" = \"form_distributions\".\"firm_id\" AND (");
            if !crate::engine::sqlx_postgres::firms::push_firm_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
    }
    if let Some(rel) = &w.created_by {
        if let Some(sub) = &rel.is {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("EXISTS (SELECT 1 FROM \"users\" t WHERE t.\"id\" = \"form_distributions\".\"created_by_id\" AND (");
            if !crate::engine::sqlx_postgres::users::push_user_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
        if let Some(sub) = &rel.is_not {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("NOT EXISTS (SELECT 1 FROM \"users\" t WHERE t.\"id\" = \"form_distributions\".\"created_by_id\" AND (");
            if !crate::engine::sqlx_postgres::users::push_user_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
    }
    if let Some(rel) = &w.submissions {
        if let Some(sub) = &rel.every {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("NOT EXISTS (SELECT 1 FROM \"form_submissions\" t WHERE t.\"distribution_id\" = \"form_distributions\".\"id\" AND NOT (");
            if !crate::engine::sqlx_postgres::forms::push_form_submission_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
        if let Some(sub) = &rel.some {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("EXISTS (SELECT 1 FROM \"form_submissions\" t WHERE t.\"distribution_id\" = \"form_distributions\".\"id\" AND (");
            if !crate::engine::sqlx_postgres::forms::push_form_submission_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
        if let Some(sub) = &rel.none {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("NOT EXISTS (SELECT 1 FROM \"form_submissions\" t WHERE t.\"distribution_id\" = \"form_distributions\".\"id\" AND (");
            if !crate::engine::sqlx_postgres::forms::push_form_submission_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
    }
    if let Some(group) = &w.and {
        for sub in group {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("(");
            if !push_form_distribution_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push(")");
        }
    }
    if let Some(group) = &w.or {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("(");
        let mut first = true;
        for sub in group {
            if !first {
                qb.push(" OR ");
            }
            first = false;
            qb.push("(");
            if !push_form_distribution_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push(")");
        }
        qb.push(")");
    }
    if let Some(group) = &w.not {
        for sub in group {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("NOT (");
            if !push_form_distribution_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push(")");
        }
    }
    any
}

impl crate::forms::FormDistribution {
    pub async fn find_unique<'e, E>(
        executor: E,
        w: &crate::forms::FormDistributionWhereUniqueInput,
    ) -> sqlx::Result<Option<Self>>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let cols = "\"id\", \"template_id\", \"template_version_id\", \"firm_id\", \"client_email\", \"client_name\", \"token\", \"expires_at\", \"status\", \"single_use\", \"created_by_id\", \"created_at\", \"submitted_at\"";
        let mut qb = sqlx::QueryBuilder::new(format!(
            r#"SELECT {} FROM "form_distributions" WHERE "#,
            cols,
        ));
        // For v1, support only the @id field. Composite uniques are out of scope.
        // The WhereUniqueInput has Option<T> per @id/@unique field; we pick the
        // first Some(). Multi-Some inputs cover composite uniques — TODO Task v2.
        if let Some(value) = &w.id {
            qb.push("\"id\" = ");
            qb.push_bind(value.clone());
        } else {
            return Ok(None);
        }
        qb.build_query_as::<Self>().fetch_optional(executor).await
    }
}

impl crate::forms::FormDistribution {
    pub async fn find_first<'e, E>(
        executor: E,
        w: &crate::forms::FormDistributionWhereInput,
    ) -> sqlx::Result<Option<Self>>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut qb = sqlx::QueryBuilder::new(
            r#"SELECT "id", "template_id", "template_version_id", "firm_id", "client_email", "client_name", "token", "expires_at", "status", "single_use", "created_by_id", "created_at", "submitted_at" FROM "form_distributions" WHERE "#,
        );
        if !crate::engine::sqlx_postgres::forms::push_form_distribution_where(&mut qb, w) {
            qb.push("TRUE");
        }
        qb.push(" LIMIT 1");
        qb.build_query_as::<Self>().fetch_optional(executor).await
    }
}

pub struct FormDistributionFindManyBuilder<'a, E> {
    executor: E,
    where_input: &'a crate::forms::FormDistributionWhereInput,
    order_by: Vec<crate::forms::FormDistributionOrderByWithRelationInput>,
    take: Option<i64>,
    skip: Option<i64>,
}

impl<'a, E> FormDistributionFindManyBuilder<'a, E> {
    pub fn order_by(
        mut self,
        order_by: &[crate::forms::FormDistributionOrderByWithRelationInput],
    ) -> Self {
        self.order_by = order_by.to_vec();
        self
    }

    pub fn take(mut self, n: i64) -> Self {
        self.take = Some(n);
        self
    }

    pub fn skip(mut self, n: i64) -> Self {
        self.skip = Some(n);
        self
    }

    pub async fn exec<'e>(self) -> sqlx::Result<Vec<crate::forms::FormDistribution>>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut qb = sqlx::QueryBuilder::new(
            r#"SELECT "id", "template_id", "template_version_id", "firm_id", "client_email", "client_name", "token", "expires_at", "status", "single_use", "created_by_id", "created_at", "submitted_at" FROM "form_distributions" WHERE "#,
        );
        if !crate::engine::sqlx_postgres::forms::push_form_distribution_where(
            &mut qb,
            self.where_input,
        ) {
            qb.push("TRUE");
        }
        if !self.order_by.is_empty() {
            qb.push(" ORDER BY ");
            let mut first = true;
            for ob in &self.order_by {
                if let Some(o) = ob.id {
                    if !first {
                        qb.push(", ");
                    }
                    first = false;
                    qb.push("\"id\" ");
                    qb.push(crate::engine::sqlx_postgres::filters::sort_order_sql(o));
                }
                if let Some(o) = ob.template_id {
                    if !first {
                        qb.push(", ");
                    }
                    first = false;
                    qb.push("\"template_id\" ");
                    qb.push(crate::engine::sqlx_postgres::filters::sort_order_sql(o));
                }
                if let Some(o) = ob.template_version_id {
                    if !first {
                        qb.push(", ");
                    }
                    first = false;
                    qb.push("\"template_version_id\" ");
                    qb.push(crate::engine::sqlx_postgres::filters::sort_order_sql(o));
                }
                if let Some(o) = ob.firm_id {
                    if !first {
                        qb.push(", ");
                    }
                    first = false;
                    qb.push("\"firm_id\" ");
                    qb.push(crate::engine::sqlx_postgres::filters::sort_order_sql(o));
                }
                if let Some(o) = ob.client_email {
                    if !first {
                        qb.push(", ");
                    }
                    first = false;
                    qb.push("\"client_email\" ");
                    qb.push(crate::engine::sqlx_postgres::filters::sort_order_sql(o));
                }
                if let Some(o) = ob.client_name {
                    if !first {
                        qb.push(", ");
                    }
                    first = false;
                    qb.push("\"client_name\" ");
                    qb.push(crate::engine::sqlx_postgres::filters::sort_order_sql(o));
                }
                if let Some(o) = ob.token {
                    if !first {
                        qb.push(", ");
                    }
                    first = false;
                    qb.push("\"token\" ");
                    qb.push(crate::engine::sqlx_postgres::filters::sort_order_sql(o));
                }
                if let Some(o) = ob.expires_at {
                    if !first {
                        qb.push(", ");
                    }
                    first = false;
                    qb.push("\"expires_at\" ");
                    qb.push(crate::engine::sqlx_postgres::filters::sort_order_sql(o));
                }
                if let Some(o) = ob.status {
                    if !first {
                        qb.push(", ");
                    }
                    first = false;
                    qb.push("\"status\" ");
                    qb.push(crate::engine::sqlx_postgres::filters::sort_order_sql(o));
                }
                if let Some(o) = ob.single_use {
                    if !first {
                        qb.push(", ");
                    }
                    first = false;
                    qb.push("\"single_use\" ");
                    qb.push(crate::engine::sqlx_postgres::filters::sort_order_sql(o));
                }
                if let Some(o) = ob.created_by_id {
                    if !first {
                        qb.push(", ");
                    }
                    first = false;
                    qb.push("\"created_by_id\" ");
                    qb.push(crate::engine::sqlx_postgres::filters::sort_order_sql(o));
                }
                if let Some(o) = ob.created_at {
                    if !first {
                        qb.push(", ");
                    }
                    first = false;
                    qb.push("\"created_at\" ");
                    qb.push(crate::engine::sqlx_postgres::filters::sort_order_sql(o));
                }
                if let Some(o) = ob.submitted_at {
                    if !first {
                        qb.push(", ");
                    }
                    first = false;
                    qb.push("\"submitted_at\" ");
                    qb.push(crate::engine::sqlx_postgres::filters::sort_order_sql(o));
                }
                // TODO(prisma-rust-generator): sorting by relation `template` not yet supported.
                let _ = &ob.template;
                // TODO(prisma-rust-generator): sorting by relation `templateVersion` not yet supported.
                let _ = &ob.template_version;
                // TODO(prisma-rust-generator): sorting by relation `firm` not yet supported.
                let _ = &ob.firm;
                // TODO(prisma-rust-generator): sorting by relation `createdBy` not yet supported.
                let _ = &ob.created_by;
                // TODO(prisma-rust-generator): sorting by relation `submissions` not yet supported.
                let _ = &ob.submissions;
            }
        }
        if let Some(n) = self.take {
            qb.push(" LIMIT ");
            qb.push_bind(n);
        }
        if let Some(n) = self.skip {
            qb.push(" OFFSET ");
            qb.push_bind(n);
        }
        qb.build_query_as::<crate::forms::FormDistribution>()
            .fetch_all(self.executor)
            .await
    }
}

impl crate::forms::FormDistribution {
    pub fn find_many<'a, E>(
        executor: E,
        w: &'a crate::forms::FormDistributionWhereInput,
    ) -> FormDistributionFindManyBuilder<'a, E> {
        FormDistributionFindManyBuilder {
            executor,
            where_input: w,
            order_by: Vec::new(),
            take: None,
            skip: None,
        }
    }
}

impl crate::forms::FormDistribution {
    pub async fn count<'e, E>(
        executor: E,
        w: Option<&crate::forms::FormDistributionWhereInput>,
    ) -> sqlx::Result<i64>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut qb = sqlx::QueryBuilder::new(r#"SELECT COUNT(*) FROM "form_distributions" WHERE "#);
        if let Some(w) = w {
            if !crate::engine::sqlx_postgres::forms::push_form_distribution_where(&mut qb, w) {
                qb.push("TRUE");
            }
        } else {
            qb.push("TRUE");
        }
        let row = qb.build_query_as::<(i64,)>().fetch_one(executor).await?;
        Ok(row.0)
    }
}

impl crate::forms::FormDistribution {
    pub async fn create<'e, E>(
        executor: E,
        input: crate::forms::FormDistributionUncheckedCreateInput,
    ) -> sqlx::Result<Self>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut cols = String::new();
        cols.push_str("\"template_id\"");
        cols.push_str(", ");
        cols.push_str("\"template_version_id\"");
        cols.push_str(", ");
        cols.push_str("\"firm_id\"");
        cols.push_str(", ");
        cols.push_str("\"client_email\"");
        cols.push_str(", ");
        cols.push_str("\"client_name\"");
        cols.push_str(", ");
        cols.push_str("\"token\"");
        cols.push_str(", ");
        cols.push_str("\"expires_at\"");
        cols.push_str(", ");
        cols.push_str("\"created_by_id\"");
        if input.submitted_at.is_some() {
            cols.push_str(", ");
            cols.push_str("\"submitted_at\"");
        }
        let mut qb = sqlx::QueryBuilder::new(format!(
            r#"INSERT INTO "form_distributions" ({}) VALUES ("#,
            cols,
        ));
        qb.push_bind(input.template_id);
        qb.push(", ");
        qb.push_bind(input.template_version_id);
        qb.push(", ");
        qb.push_bind(input.firm_id);
        qb.push(", ");
        qb.push_bind(input.client_email);
        qb.push(", ");
        qb.push_bind(input.client_name);
        qb.push(", ");
        qb.push_bind(input.token);
        qb.push(", ");
        qb.push_bind(input.expires_at);
        qb.push(", ");
        qb.push_bind(input.created_by_id);
        if let Some(v) = input.submitted_at {
            qb.push(", ");
            qb.push_bind(v);
        }
        qb.push(r#") RETURNING "id", "template_id", "template_version_id", "firm_id", "client_email", "client_name", "token", "expires_at", "status", "single_use", "created_by_id", "created_at", "submitted_at""#);
        qb.build_query_as::<Self>().fetch_one(executor).await
    }
}

impl crate::forms::FormDistribution {
    pub async fn create_many<'e, E>(
        executor: E,
        inputs: &[crate::forms::FormDistributionUncheckedCreateInput],
    ) -> sqlx::Result<u64>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        if inputs.is_empty() {
            return Ok(0);
        }
        let mut qb = sqlx::QueryBuilder::new(
            r#"INSERT INTO "form_distributions" ("template_id", "template_version_id", "firm_id", "client_email", "client_name", "token", "expires_at", "created_by_id", "submitted_at") "#,
        );
        qb.push_values(inputs, |mut row, input| {
            row.push_bind(input.template_id.clone());
            row.push_bind(input.template_version_id.clone());
            row.push_bind(input.firm_id.clone());
            row.push_bind(input.client_email.clone());
            row.push_bind(input.client_name.clone());
            row.push_bind(input.token.clone());
            row.push_bind(input.expires_at);
            row.push_bind(input.created_by_id.clone());
            row.push_bind(input.submitted_at);
        });
        let result = qb.build().execute(executor).await?;
        Ok(result.rows_affected())
    }
}

impl crate::forms::FormDistribution {
    pub async fn update<'e, E>(
        executor: E,
        w: &crate::forms::FormDistributionWhereUniqueInput,
        input: crate::forms::FormDistributionUncheckedUpdateInput,
    ) -> sqlx::Result<Self>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut qb = sqlx::QueryBuilder::new(r#"UPDATE "form_distributions" SET "#);
        let mut first = true;
        if let Some(op) = input.id {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"id\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.template_id {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"template_id\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.template_version_id {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"template_version_id\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.firm_id {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"firm_id\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.client_email {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"client_email\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.client_name {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"client_name\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.token {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"token\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.expires_at {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"expires_at\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.status {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"status\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.single_use {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"single_use\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.created_by_id {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"created_by_id\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.created_at {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"created_at\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.submitted_at {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"submitted_at\" = ");
                qb.push_bind(v);
            }
        }
        if first {
            // Nothing to update — fall back to a plain find_unique so the
            // caller still gets the row back (or RowNotFound if absent).
            return Self::find_unique(executor, w)
                .await?
                .ok_or(sqlx::Error::RowNotFound);
        }
        qb.push(" WHERE ");
        if let Some(value) = &w.id {
            qb.push("\"id\" = ");
            qb.push_bind(value.clone());
        } else {
            return Err(sqlx::Error::RowNotFound);
        }
        qb.push(r#" RETURNING "id", "template_id", "template_version_id", "firm_id", "client_email", "client_name", "token", "expires_at", "status", "single_use", "created_by_id", "created_at", "submitted_at""#);
        qb.build_query_as::<Self>().fetch_one(executor).await
    }
}

impl crate::forms::FormDistribution {
    pub async fn update_many<'e, E>(
        executor: E,
        w: &crate::forms::FormDistributionWhereInput,
        input: crate::forms::FormDistributionUncheckedUpdateManyInput,
    ) -> sqlx::Result<u64>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut qb = sqlx::QueryBuilder::new(r#"UPDATE "form_distributions" SET "#);
        let mut first = true;
        if let Some(op) = input.id {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"id\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.template_id {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"template_id\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.template_version_id {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"template_version_id\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.firm_id {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"firm_id\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.client_email {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"client_email\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.client_name {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"client_name\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.token {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"token\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.expires_at {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"expires_at\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.status {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"status\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.single_use {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"single_use\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.created_by_id {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"created_by_id\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.created_at {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"created_at\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.submitted_at {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"submitted_at\" = ");
                qb.push_bind(v);
            }
        }
        if first {
            // No-op update — return 0 rows affected without issuing SQL.
            return Ok(0);
        }
        qb.push(" WHERE ");
        if !crate::engine::sqlx_postgres::forms::push_form_distribution_where(&mut qb, w) {
            qb.push("TRUE");
        }
        let result = qb.build().execute(executor).await?;
        Ok(result.rows_affected())
    }
}

impl crate::forms::FormDistribution {
    pub async fn delete<'e, E>(
        executor: E,
        w: &crate::forms::FormDistributionWhereUniqueInput,
    ) -> sqlx::Result<Self>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut qb = sqlx::QueryBuilder::new(r#"DELETE FROM "form_distributions" WHERE "#);
        if let Some(value) = &w.id {
            qb.push("\"id\" = ");
            qb.push_bind(value.clone());
        } else {
            return Err(sqlx::Error::RowNotFound);
        }
        qb.push(r#" RETURNING "id", "template_id", "template_version_id", "firm_id", "client_email", "client_name", "token", "expires_at", "status", "single_use", "created_by_id", "created_at", "submitted_at""#);
        qb.build_query_as::<Self>().fetch_one(executor).await
    }
}

impl crate::forms::FormDistribution {
    pub async fn delete_many<'e, E>(
        executor: E,
        w: &crate::forms::FormDistributionWhereInput,
    ) -> sqlx::Result<u64>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut qb = sqlx::QueryBuilder::new(r#"DELETE FROM "form_distributions" WHERE "#);
        if !crate::engine::sqlx_postgres::forms::push_form_distribution_where(&mut qb, w) {
            qb.push("TRUE");
        }
        let result = qb.build().execute(executor).await?;
        Ok(result.rows_affected())
    }
}

#[derive(Debug, Clone, Default, PartialEq, Eq, Hash, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct FormDistributionCountAggregateResult {
    pub id: Option<i64>,
    pub template_id: Option<i64>,
    pub template_version_id: Option<i64>,
    pub firm_id: Option<i64>,
    pub client_email: Option<i64>,
    pub client_name: Option<i64>,
    pub token: Option<i64>,
    pub expires_at: Option<i64>,
    pub status: Option<i64>,
    pub single_use: Option<i64>,
    pub created_by_id: Option<i64>,
    pub created_at: Option<i64>,
    pub submitted_at: Option<i64>,
    #[serde(rename = "_all")]
    pub aggregate_all: Option<i64>,
}

#[derive(Debug, Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct FormDistributionAvgAggregateResult {}

#[derive(Debug, Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct FormDistributionSumAggregateResult {}

#[derive(Debug, Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct FormDistributionMinAggregateResult {
    pub id: Option<String>,
    pub template_id: Option<String>,
    pub template_version_id: Option<String>,
    pub firm_id: Option<String>,
    pub client_email: Option<String>,
    pub client_name: Option<String>,
    pub token: Option<String>,
    pub expires_at: Option<chrono::DateTime<chrono::Utc>>,
    pub single_use: Option<bool>,
    pub created_by_id: Option<String>,
    pub created_at: Option<chrono::DateTime<chrono::Utc>>,
    pub submitted_at: Option<chrono::DateTime<chrono::Utc>>,
}

#[derive(Debug, Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct FormDistributionMaxAggregateResult {
    pub id: Option<String>,
    pub template_id: Option<String>,
    pub template_version_id: Option<String>,
    pub firm_id: Option<String>,
    pub client_email: Option<String>,
    pub client_name: Option<String>,
    pub token: Option<String>,
    pub expires_at: Option<chrono::DateTime<chrono::Utc>>,
    pub single_use: Option<bool>,
    pub created_by_id: Option<String>,
    pub created_at: Option<chrono::DateTime<chrono::Utc>>,
    pub submitted_at: Option<chrono::DateTime<chrono::Utc>>,
}

#[derive(Debug, Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct FormDistributionAggregateResult {
    #[serde(rename = "_count")]
    pub aggregate_count: Option<FormDistributionCountAggregateResult>,
    #[serde(rename = "_avg")]
    pub aggregate_avg: Option<FormDistributionAvgAggregateResult>,
    #[serde(rename = "_sum")]
    pub aggregate_sum: Option<FormDistributionSumAggregateResult>,
    #[serde(rename = "_min")]
    pub aggregate_min: Option<FormDistributionMinAggregateResult>,
    #[serde(rename = "_max")]
    pub aggregate_max: Option<FormDistributionMaxAggregateResult>,
}

impl crate::forms::FormDistribution {
    pub async fn aggregate<'e, E>(
        executor: E,
        input: &crate::forms::FormDistributionAggregateInput,
    ) -> sqlx::Result<FormDistributionAggregateResult>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut qb = sqlx::QueryBuilder::new(
            r#"SELECT COUNT(*) AS count_all, COUNT("id") AS count_id, COUNT("template_id") AS count_template_id, COUNT("template_version_id") AS count_template_version_id, COUNT("firm_id") AS count_firm_id, COUNT("client_email") AS count_client_email, COUNT("client_name") AS count_client_name, COUNT("token") AS count_token, COUNT("expires_at") AS count_expires_at, COUNT("status") AS count_status, COUNT("single_use") AS count_single_use, COUNT("created_by_id") AS count_created_by_id, COUNT("created_at") AS count_created_at, COUNT("submitted_at") AS count_submitted_at, MIN("id") AS min_id, MAX("id") AS max_id, MIN("template_id") AS min_template_id, MAX("template_id") AS max_template_id, MIN("template_version_id") AS min_template_version_id, MAX("template_version_id") AS max_template_version_id, MIN("firm_id") AS min_firm_id, MAX("firm_id") AS max_firm_id, MIN("client_email") AS min_client_email, MAX("client_email") AS max_client_email, MIN("client_name") AS min_client_name, MAX("client_name") AS max_client_name, MIN("token") AS min_token, MAX("token") AS max_token, MIN("expires_at") AS min_expires_at, MAX("expires_at") AS max_expires_at, MIN("single_use") AS min_single_use, MAX("single_use") AS max_single_use, MIN("created_by_id") AS min_created_by_id, MAX("created_by_id") AS max_created_by_id, MIN("created_at") AS min_created_at, MAX("created_at") AS max_created_at, MIN("submitted_at") AS min_submitted_at, MAX("submitted_at") AS max_submitted_at FROM "form_distributions" WHERE "#,
        );
        if let Some(w) = &input.r#where {
            if !crate::engine::sqlx_postgres::forms::push_form_distribution_where(&mut qb, w) {
                qb.push("TRUE");
            }
        } else {
            qb.push("TRUE");
        }
        let row = qb.build().fetch_one(executor).await?;
        use sqlx::Row;
        Ok(FormDistributionAggregateResult {
            aggregate_count: input
                .aggregate_count
                .as_ref()
                .map(|req| -> sqlx::Result<_> {
                    Ok(FormDistributionCountAggregateResult {
                        id: if req.id.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_id")?
                        } else {
                            None
                        },
                        template_id: if req.template_id.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_template_id")?
                        } else {
                            None
                        },
                        template_version_id: if req.template_version_id.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_template_version_id")?
                        } else {
                            None
                        },
                        firm_id: if req.firm_id.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_firm_id")?
                        } else {
                            None
                        },
                        client_email: if req.client_email.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_client_email")?
                        } else {
                            None
                        },
                        client_name: if req.client_name.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_client_name")?
                        } else {
                            None
                        },
                        token: if req.token.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_token")?
                        } else {
                            None
                        },
                        expires_at: if req.expires_at.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_expires_at")?
                        } else {
                            None
                        },
                        status: if req.status.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_status")?
                        } else {
                            None
                        },
                        single_use: if req.single_use.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_single_use")?
                        } else {
                            None
                        },
                        created_by_id: if req.created_by_id.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_created_by_id")?
                        } else {
                            None
                        },
                        created_at: if req.created_at.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_created_at")?
                        } else {
                            None
                        },
                        submitted_at: if req.submitted_at.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_submitted_at")?
                        } else {
                            None
                        },
                        aggregate_all: if req.aggregate_all.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_all")?
                        } else {
                            None
                        },
                    })
                })
                .transpose()?,
            aggregate_avg: input
                .aggregate_avg
                .as_ref()
                .map(|_req| -> sqlx::Result<_> { Ok(FormDistributionAvgAggregateResult {}) })
                .transpose()?,
            aggregate_sum: input
                .aggregate_sum
                .as_ref()
                .map(|_req| -> sqlx::Result<_> { Ok(FormDistributionSumAggregateResult {}) })
                .transpose()?,
            aggregate_min: input
                .aggregate_min
                .as_ref()
                .map(|req| -> sqlx::Result<_> {
                    Ok(FormDistributionMinAggregateResult {
                        id: if req.id.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("min_id")?
                        } else {
                            None
                        },
                        template_id: if req.template_id.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("min_template_id")?
                        } else {
                            None
                        },
                        template_version_id: if req.template_version_id.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("min_template_version_id")?
                        } else {
                            None
                        },
                        firm_id: if req.firm_id.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("min_firm_id")?
                        } else {
                            None
                        },
                        client_email: if req.client_email.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("min_client_email")?
                        } else {
                            None
                        },
                        client_name: if req.client_name.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("min_client_name")?
                        } else {
                            None
                        },
                        token: if req.token.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("min_token")?
                        } else {
                            None
                        },
                        expires_at: if req.expires_at.unwrap_or(false) {
                            row.try_get::<Option<chrono::DateTime<chrono::Utc>>, _>(
                                "min_expires_at",
                            )?
                        } else {
                            None
                        },
                        single_use: if req.single_use.unwrap_or(false) {
                            row.try_get::<Option<bool>, _>("min_single_use")?
                        } else {
                            None
                        },
                        created_by_id: if req.created_by_id.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("min_created_by_id")?
                        } else {
                            None
                        },
                        created_at: if req.created_at.unwrap_or(false) {
                            row.try_get::<Option<chrono::DateTime<chrono::Utc>>, _>(
                                "min_created_at",
                            )?
                        } else {
                            None
                        },
                        submitted_at: if req.submitted_at.unwrap_or(false) {
                            row.try_get::<Option<chrono::DateTime<chrono::Utc>>, _>(
                                "min_submitted_at",
                            )?
                        } else {
                            None
                        },
                    })
                })
                .transpose()?,
            aggregate_max: input
                .aggregate_max
                .as_ref()
                .map(|req| -> sqlx::Result<_> {
                    Ok(FormDistributionMaxAggregateResult {
                        id: if req.id.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("max_id")?
                        } else {
                            None
                        },
                        template_id: if req.template_id.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("max_template_id")?
                        } else {
                            None
                        },
                        template_version_id: if req.template_version_id.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("max_template_version_id")?
                        } else {
                            None
                        },
                        firm_id: if req.firm_id.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("max_firm_id")?
                        } else {
                            None
                        },
                        client_email: if req.client_email.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("max_client_email")?
                        } else {
                            None
                        },
                        client_name: if req.client_name.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("max_client_name")?
                        } else {
                            None
                        },
                        token: if req.token.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("max_token")?
                        } else {
                            None
                        },
                        expires_at: if req.expires_at.unwrap_or(false) {
                            row.try_get::<Option<chrono::DateTime<chrono::Utc>>, _>(
                                "max_expires_at",
                            )?
                        } else {
                            None
                        },
                        single_use: if req.single_use.unwrap_or(false) {
                            row.try_get::<Option<bool>, _>("max_single_use")?
                        } else {
                            None
                        },
                        created_by_id: if req.created_by_id.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("max_created_by_id")?
                        } else {
                            None
                        },
                        created_at: if req.created_at.unwrap_or(false) {
                            row.try_get::<Option<chrono::DateTime<chrono::Utc>>, _>(
                                "max_created_at",
                            )?
                        } else {
                            None
                        },
                        submitted_at: if req.submitted_at.unwrap_or(false) {
                            row.try_get::<Option<chrono::DateTime<chrono::Utc>>, _>(
                                "max_submitted_at",
                            )?
                        } else {
                            None
                        },
                    })
                })
                .transpose()?,
        })
    }
}

impl sqlx::FromRow<'_, sqlx::postgres::PgRow> for crate::forms::FormSubmission {
    fn from_row(row: &sqlx::postgres::PgRow) -> sqlx::Result<Self> {
        Ok(crate::forms::FormSubmission {
            id: row.try_get("id")?,
            distribution_id: row.try_get("distribution_id")?,
            template_id: row.try_get("template_id")?,
            template_version_id: row.try_get("template_version_id")?,
            firm_id: row.try_get("firm_id")?,
            data: row.try_get("data")?,
            status: row.try_get("status")?,
            submitted_at: row.try_get("submitted_at")?,
            verified_at: row.try_get("verified_at")?,
            verified_by_id: row.try_get("verified_by_id")?,
            distribution: None,
            template: None,
            template_version: None,
            firm: None,
            verified_by: None,
            notes: Vec::new(),
            pdf_uploads: Vec::new(),
        })
    }
}

pub(crate) fn push_form_submission_where(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    w: &crate::forms::FormSubmissionWhereInput,
) -> bool {
    let mut any = false;
    if let Some(f) = &w.id {
        if any {
            qb.push(" AND ");
        }
        any = true;
        crate::engine::sqlx_postgres::filters::push_string_filter(qb, "id", f);
    }
    if let Some(f) = &w.distribution_id {
        if any {
            qb.push(" AND ");
        }
        any = true;
        crate::engine::sqlx_postgres::filters::push_string_filter(qb, "distribution_id", f);
    }
    if let Some(f) = &w.template_id {
        if any {
            qb.push(" AND ");
        }
        any = true;
        crate::engine::sqlx_postgres::filters::push_string_filter(qb, "template_id", f);
    }
    if let Some(f) = &w.template_version_id {
        if any {
            qb.push(" AND ");
        }
        any = true;
        crate::engine::sqlx_postgres::filters::push_string_filter(qb, "template_version_id", f);
    }
    if let Some(f) = &w.firm_id {
        if any {
            qb.push(" AND ");
        }
        any = true;
        crate::engine::sqlx_postgres::filters::push_string_filter(qb, "firm_id", f);
    }
    if let Some(f) = &w.data {
        if any {
            qb.push(" AND ");
        }
        any = true;
        crate::engine::sqlx_postgres::filters::push_json_filter(qb, "data", f);
    }
    if let Some(f) = &w.status {
        if any {
            qb.push(" AND ");
        }
        any = true;
        crate::engine::sqlx_postgres::filters::push_submission_status_filter(qb, "status", f);
    }
    if let Some(f) = &w.submitted_at {
        if any {
            qb.push(" AND ");
        }
        any = true;
        crate::engine::sqlx_postgres::filters::push_datetime_filter(qb, "submitted_at", f);
    }
    if let Some(f) = &w.verified_at {
        if any {
            qb.push(" AND ");
        }
        any = true;
        crate::engine::sqlx_postgres::filters::push_datetime_nullable_filter(qb, "verified_at", f);
    }
    if let Some(f) = &w.verified_by_id {
        if any {
            qb.push(" AND ");
        }
        any = true;
        crate::engine::sqlx_postgres::filters::push_string_nullable_filter(qb, "verified_by_id", f);
    }
    if let Some(rel) = &w.distribution {
        if let Some(sub) = &rel.is {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("EXISTS (SELECT 1 FROM \"form_distributions\" t WHERE t.\"id\" = \"form_submissions\".\"distribution_id\" AND (");
            if !crate::engine::sqlx_postgres::forms::push_form_distribution_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
        if let Some(sub) = &rel.is_not {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("NOT EXISTS (SELECT 1 FROM \"form_distributions\" t WHERE t.\"id\" = \"form_submissions\".\"distribution_id\" AND (");
            if !crate::engine::sqlx_postgres::forms::push_form_distribution_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
    }
    if let Some(rel) = &w.template {
        if let Some(sub) = &rel.is {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("EXISTS (SELECT 1 FROM \"form_templates\" t WHERE t.\"id\" = \"form_submissions\".\"template_id\" AND (");
            if !crate::engine::sqlx_postgres::forms::push_form_template_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
        if let Some(sub) = &rel.is_not {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("NOT EXISTS (SELECT 1 FROM \"form_templates\" t WHERE t.\"id\" = \"form_submissions\".\"template_id\" AND (");
            if !crate::engine::sqlx_postgres::forms::push_form_template_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
    }
    if let Some(rel) = &w.template_version {
        if let Some(sub) = &rel.is {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("EXISTS (SELECT 1 FROM \"form_template_versions\" t WHERE t.\"id\" = \"form_submissions\".\"template_version_id\" AND (");
            if !crate::engine::sqlx_postgres::forms::push_form_template_version_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
        if let Some(sub) = &rel.is_not {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("NOT EXISTS (SELECT 1 FROM \"form_template_versions\" t WHERE t.\"id\" = \"form_submissions\".\"template_version_id\" AND (");
            if !crate::engine::sqlx_postgres::forms::push_form_template_version_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
    }
    if let Some(rel) = &w.firm {
        if let Some(sub) = &rel.is {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("EXISTS (SELECT 1 FROM \"firms\" t WHERE t.\"id\" = \"form_submissions\".\"firm_id\" AND (");
            if !crate::engine::sqlx_postgres::firms::push_firm_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
        if let Some(sub) = &rel.is_not {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("NOT EXISTS (SELECT 1 FROM \"firms\" t WHERE t.\"id\" = \"form_submissions\".\"firm_id\" AND (");
            if !crate::engine::sqlx_postgres::firms::push_firm_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
    }
    if let Some(rel) = &w.verified_by {
        if let Some(sub) = &rel.is {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("EXISTS (SELECT 1 FROM \"users\" t WHERE t.\"id\" = \"form_submissions\".\"verified_by_id\" AND (");
            if !crate::engine::sqlx_postgres::users::push_user_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
        if let Some(sub) = &rel.is_not {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("NOT EXISTS (SELECT 1 FROM \"users\" t WHERE t.\"id\" = \"form_submissions\".\"verified_by_id\" AND (");
            if !crate::engine::sqlx_postgres::users::push_user_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
    }
    if let Some(rel) = &w.notes {
        if let Some(sub) = &rel.every {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("NOT EXISTS (SELECT 1 FROM \"submission_notes\" t WHERE t.\"submission_id\" = \"form_submissions\".\"id\" AND NOT (");
            if !crate::engine::sqlx_postgres::forms::push_submission_note_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
        if let Some(sub) = &rel.some {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("EXISTS (SELECT 1 FROM \"submission_notes\" t WHERE t.\"submission_id\" = \"form_submissions\".\"id\" AND (");
            if !crate::engine::sqlx_postgres::forms::push_submission_note_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
        if let Some(sub) = &rel.none {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("NOT EXISTS (SELECT 1 FROM \"submission_notes\" t WHERE t.\"submission_id\" = \"form_submissions\".\"id\" AND (");
            if !crate::engine::sqlx_postgres::forms::push_submission_note_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
    }
    if let Some(rel) = &w.pdf_uploads {
        if let Some(sub) = &rel.every {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("NOT EXISTS (SELECT 1 FROM \"pdf_uploads\" t WHERE t.\"submission_id\" = \"form_submissions\".\"id\" AND NOT (");
            if !crate::engine::sqlx_postgres::pdf_processing::push_pdf_upload_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
        if let Some(sub) = &rel.some {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("EXISTS (SELECT 1 FROM \"pdf_uploads\" t WHERE t.\"submission_id\" = \"form_submissions\".\"id\" AND (");
            if !crate::engine::sqlx_postgres::pdf_processing::push_pdf_upload_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
        if let Some(sub) = &rel.none {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("NOT EXISTS (SELECT 1 FROM \"pdf_uploads\" t WHERE t.\"submission_id\" = \"form_submissions\".\"id\" AND (");
            if !crate::engine::sqlx_postgres::pdf_processing::push_pdf_upload_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
    }
    if let Some(group) = &w.and {
        for sub in group {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("(");
            if !push_form_submission_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push(")");
        }
    }
    if let Some(group) = &w.or {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("(");
        let mut first = true;
        for sub in group {
            if !first {
                qb.push(" OR ");
            }
            first = false;
            qb.push("(");
            if !push_form_submission_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push(")");
        }
        qb.push(")");
    }
    if let Some(group) = &w.not {
        for sub in group {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("NOT (");
            if !push_form_submission_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push(")");
        }
    }
    any
}

impl crate::forms::FormSubmission {
    pub async fn find_unique<'e, E>(
        executor: E,
        w: &crate::forms::FormSubmissionWhereUniqueInput,
    ) -> sqlx::Result<Option<Self>>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let cols = "\"id\", \"distribution_id\", \"template_id\", \"template_version_id\", \"firm_id\", \"data\", \"status\", \"submitted_at\", \"verified_at\", \"verified_by_id\"";
        let mut qb =
            sqlx::QueryBuilder::new(format!(r#"SELECT {} FROM "form_submissions" WHERE "#, cols,));
        // For v1, support only the @id field. Composite uniques are out of scope.
        // The WhereUniqueInput has Option<T> per @id/@unique field; we pick the
        // first Some(). Multi-Some inputs cover composite uniques — TODO Task v2.
        if let Some(value) = &w.id {
            qb.push("\"id\" = ");
            qb.push_bind(value.clone());
        } else {
            return Ok(None);
        }
        qb.build_query_as::<Self>().fetch_optional(executor).await
    }
}

impl crate::forms::FormSubmission {
    pub async fn find_first<'e, E>(
        executor: E,
        w: &crate::forms::FormSubmissionWhereInput,
    ) -> sqlx::Result<Option<Self>>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut qb = sqlx::QueryBuilder::new(
            r#"SELECT "id", "distribution_id", "template_id", "template_version_id", "firm_id", "data", "status", "submitted_at", "verified_at", "verified_by_id" FROM "form_submissions" WHERE "#,
        );
        if !crate::engine::sqlx_postgres::forms::push_form_submission_where(&mut qb, w) {
            qb.push("TRUE");
        }
        qb.push(" LIMIT 1");
        qb.build_query_as::<Self>().fetch_optional(executor).await
    }
}

pub struct FormSubmissionFindManyBuilder<'a, E> {
    executor: E,
    where_input: &'a crate::forms::FormSubmissionWhereInput,
    order_by: Vec<crate::forms::FormSubmissionOrderByWithRelationInput>,
    take: Option<i64>,
    skip: Option<i64>,
}

impl<'a, E> FormSubmissionFindManyBuilder<'a, E> {
    pub fn order_by(
        mut self,
        order_by: &[crate::forms::FormSubmissionOrderByWithRelationInput],
    ) -> Self {
        self.order_by = order_by.to_vec();
        self
    }

    pub fn take(mut self, n: i64) -> Self {
        self.take = Some(n);
        self
    }

    pub fn skip(mut self, n: i64) -> Self {
        self.skip = Some(n);
        self
    }

    pub async fn exec<'e>(self) -> sqlx::Result<Vec<crate::forms::FormSubmission>>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut qb = sqlx::QueryBuilder::new(
            r#"SELECT "id", "distribution_id", "template_id", "template_version_id", "firm_id", "data", "status", "submitted_at", "verified_at", "verified_by_id" FROM "form_submissions" WHERE "#,
        );
        if !crate::engine::sqlx_postgres::forms::push_form_submission_where(
            &mut qb,
            self.where_input,
        ) {
            qb.push("TRUE");
        }
        if !self.order_by.is_empty() {
            qb.push(" ORDER BY ");
            let mut first = true;
            for ob in &self.order_by {
                if let Some(o) = ob.id {
                    if !first {
                        qb.push(", ");
                    }
                    first = false;
                    qb.push("\"id\" ");
                    qb.push(crate::engine::sqlx_postgres::filters::sort_order_sql(o));
                }
                if let Some(o) = ob.distribution_id {
                    if !first {
                        qb.push(", ");
                    }
                    first = false;
                    qb.push("\"distribution_id\" ");
                    qb.push(crate::engine::sqlx_postgres::filters::sort_order_sql(o));
                }
                if let Some(o) = ob.template_id {
                    if !first {
                        qb.push(", ");
                    }
                    first = false;
                    qb.push("\"template_id\" ");
                    qb.push(crate::engine::sqlx_postgres::filters::sort_order_sql(o));
                }
                if let Some(o) = ob.template_version_id {
                    if !first {
                        qb.push(", ");
                    }
                    first = false;
                    qb.push("\"template_version_id\" ");
                    qb.push(crate::engine::sqlx_postgres::filters::sort_order_sql(o));
                }
                if let Some(o) = ob.firm_id {
                    if !first {
                        qb.push(", ");
                    }
                    first = false;
                    qb.push("\"firm_id\" ");
                    qb.push(crate::engine::sqlx_postgres::filters::sort_order_sql(o));
                }
                if let Some(o) = ob.data {
                    if !first {
                        qb.push(", ");
                    }
                    first = false;
                    qb.push("\"data\" ");
                    qb.push(crate::engine::sqlx_postgres::filters::sort_order_sql(o));
                }
                if let Some(o) = ob.status {
                    if !first {
                        qb.push(", ");
                    }
                    first = false;
                    qb.push("\"status\" ");
                    qb.push(crate::engine::sqlx_postgres::filters::sort_order_sql(o));
                }
                if let Some(o) = ob.submitted_at {
                    if !first {
                        qb.push(", ");
                    }
                    first = false;
                    qb.push("\"submitted_at\" ");
                    qb.push(crate::engine::sqlx_postgres::filters::sort_order_sql(o));
                }
                if let Some(o) = ob.verified_at {
                    if !first {
                        qb.push(", ");
                    }
                    first = false;
                    qb.push("\"verified_at\" ");
                    qb.push(crate::engine::sqlx_postgres::filters::sort_order_sql(o));
                }
                if let Some(o) = ob.verified_by_id {
                    if !first {
                        qb.push(", ");
                    }
                    first = false;
                    qb.push("\"verified_by_id\" ");
                    qb.push(crate::engine::sqlx_postgres::filters::sort_order_sql(o));
                }
                // TODO(prisma-rust-generator): sorting by relation `distribution` not yet supported.
                let _ = &ob.distribution;
                // TODO(prisma-rust-generator): sorting by relation `template` not yet supported.
                let _ = &ob.template;
                // TODO(prisma-rust-generator): sorting by relation `templateVersion` not yet supported.
                let _ = &ob.template_version;
                // TODO(prisma-rust-generator): sorting by relation `firm` not yet supported.
                let _ = &ob.firm;
                // TODO(prisma-rust-generator): sorting by relation `verifiedBy` not yet supported.
                let _ = &ob.verified_by;
                // TODO(prisma-rust-generator): sorting by relation `notes` not yet supported.
                let _ = &ob.notes;
                // TODO(prisma-rust-generator): sorting by relation `pdfUploads` not yet supported.
                let _ = &ob.pdf_uploads;
            }
        }
        if let Some(n) = self.take {
            qb.push(" LIMIT ");
            qb.push_bind(n);
        }
        if let Some(n) = self.skip {
            qb.push(" OFFSET ");
            qb.push_bind(n);
        }
        qb.build_query_as::<crate::forms::FormSubmission>()
            .fetch_all(self.executor)
            .await
    }
}

impl crate::forms::FormSubmission {
    pub fn find_many<'a, E>(
        executor: E,
        w: &'a crate::forms::FormSubmissionWhereInput,
    ) -> FormSubmissionFindManyBuilder<'a, E> {
        FormSubmissionFindManyBuilder {
            executor,
            where_input: w,
            order_by: Vec::new(),
            take: None,
            skip: None,
        }
    }
}

impl crate::forms::FormSubmission {
    pub async fn count<'e, E>(
        executor: E,
        w: Option<&crate::forms::FormSubmissionWhereInput>,
    ) -> sqlx::Result<i64>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut qb = sqlx::QueryBuilder::new(r#"SELECT COUNT(*) FROM "form_submissions" WHERE "#);
        if let Some(w) = w {
            if !crate::engine::sqlx_postgres::forms::push_form_submission_where(&mut qb, w) {
                qb.push("TRUE");
            }
        } else {
            qb.push("TRUE");
        }
        let row = qb.build_query_as::<(i64,)>().fetch_one(executor).await?;
        Ok(row.0)
    }
}

impl crate::forms::FormSubmission {
    pub async fn create<'e, E>(
        executor: E,
        input: crate::forms::FormSubmissionUncheckedCreateInput,
    ) -> sqlx::Result<Self>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut cols = String::new();
        cols.push_str("\"distribution_id\"");
        cols.push_str(", ");
        cols.push_str("\"template_id\"");
        cols.push_str(", ");
        cols.push_str("\"template_version_id\"");
        cols.push_str(", ");
        cols.push_str("\"firm_id\"");
        cols.push_str(", ");
        cols.push_str("\"data\"");
        if input.verified_at.is_some() {
            cols.push_str(", ");
            cols.push_str("\"verified_at\"");
        }
        if input.verified_by_id.is_some() {
            cols.push_str(", ");
            cols.push_str("\"verified_by_id\"");
        }
        let mut qb = sqlx::QueryBuilder::new(format!(
            r#"INSERT INTO "form_submissions" ({}) VALUES ("#,
            cols,
        ));
        qb.push_bind(input.distribution_id);
        qb.push(", ");
        qb.push_bind(input.template_id);
        qb.push(", ");
        qb.push_bind(input.template_version_id);
        qb.push(", ");
        qb.push_bind(input.firm_id);
        qb.push(", ");
        qb.push_bind(input.data);
        if let Some(v) = input.verified_at {
            qb.push(", ");
            qb.push_bind(v);
        }
        if let Some(v) = input.verified_by_id {
            qb.push(", ");
            qb.push_bind(v);
        }
        qb.push(r#") RETURNING "id", "distribution_id", "template_id", "template_version_id", "firm_id", "data", "status", "submitted_at", "verified_at", "verified_by_id""#);
        qb.build_query_as::<Self>().fetch_one(executor).await
    }
}

impl crate::forms::FormSubmission {
    pub async fn create_many<'e, E>(
        executor: E,
        inputs: &[crate::forms::FormSubmissionUncheckedCreateInput],
    ) -> sqlx::Result<u64>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        if inputs.is_empty() {
            return Ok(0);
        }
        let mut qb = sqlx::QueryBuilder::new(
            r#"INSERT INTO "form_submissions" ("distribution_id", "template_id", "template_version_id", "firm_id", "data", "verified_at", "verified_by_id") "#,
        );
        qb.push_values(inputs, |mut row, input| {
            row.push_bind(input.distribution_id.clone());
            row.push_bind(input.template_id.clone());
            row.push_bind(input.template_version_id.clone());
            row.push_bind(input.firm_id.clone());
            row.push_bind(input.data.clone());
            row.push_bind(input.verified_at);
            row.push_bind(input.verified_by_id.clone());
        });
        let result = qb.build().execute(executor).await?;
        Ok(result.rows_affected())
    }
}

impl crate::forms::FormSubmission {
    pub async fn update<'e, E>(
        executor: E,
        w: &crate::forms::FormSubmissionWhereUniqueInput,
        input: crate::forms::FormSubmissionUncheckedUpdateInput,
    ) -> sqlx::Result<Self>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut qb = sqlx::QueryBuilder::new(r#"UPDATE "form_submissions" SET "#);
        let mut first = true;
        if let Some(op) = input.id {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"id\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.distribution_id {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"distribution_id\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.template_id {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"template_id\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.template_version_id {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"template_version_id\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.firm_id {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"firm_id\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.data {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"data\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.status {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"status\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.submitted_at {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"submitted_at\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.verified_at {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"verified_at\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.verified_by_id {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"verified_by_id\" = ");
                qb.push_bind(v);
            }
        }
        if first {
            // Nothing to update — fall back to a plain find_unique so the
            // caller still gets the row back (or RowNotFound if absent).
            return Self::find_unique(executor, w)
                .await?
                .ok_or(sqlx::Error::RowNotFound);
        }
        qb.push(" WHERE ");
        if let Some(value) = &w.id {
            qb.push("\"id\" = ");
            qb.push_bind(value.clone());
        } else {
            return Err(sqlx::Error::RowNotFound);
        }
        qb.push(r#" RETURNING "id", "distribution_id", "template_id", "template_version_id", "firm_id", "data", "status", "submitted_at", "verified_at", "verified_by_id""#);
        qb.build_query_as::<Self>().fetch_one(executor).await
    }
}

impl crate::forms::FormSubmission {
    pub async fn update_many<'e, E>(
        executor: E,
        w: &crate::forms::FormSubmissionWhereInput,
        input: crate::forms::FormSubmissionUncheckedUpdateManyInput,
    ) -> sqlx::Result<u64>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut qb = sqlx::QueryBuilder::new(r#"UPDATE "form_submissions" SET "#);
        let mut first = true;
        if let Some(op) = input.id {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"id\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.distribution_id {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"distribution_id\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.template_id {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"template_id\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.template_version_id {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"template_version_id\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.firm_id {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"firm_id\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.data {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"data\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.status {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"status\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.submitted_at {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"submitted_at\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.verified_at {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"verified_at\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.verified_by_id {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"verified_by_id\" = ");
                qb.push_bind(v);
            }
        }
        if first {
            // No-op update — return 0 rows affected without issuing SQL.
            return Ok(0);
        }
        qb.push(" WHERE ");
        if !crate::engine::sqlx_postgres::forms::push_form_submission_where(&mut qb, w) {
            qb.push("TRUE");
        }
        let result = qb.build().execute(executor).await?;
        Ok(result.rows_affected())
    }
}

impl crate::forms::FormSubmission {
    pub async fn delete<'e, E>(
        executor: E,
        w: &crate::forms::FormSubmissionWhereUniqueInput,
    ) -> sqlx::Result<Self>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut qb = sqlx::QueryBuilder::new(r#"DELETE FROM "form_submissions" WHERE "#);
        if let Some(value) = &w.id {
            qb.push("\"id\" = ");
            qb.push_bind(value.clone());
        } else {
            return Err(sqlx::Error::RowNotFound);
        }
        qb.push(r#" RETURNING "id", "distribution_id", "template_id", "template_version_id", "firm_id", "data", "status", "submitted_at", "verified_at", "verified_by_id""#);
        qb.build_query_as::<Self>().fetch_one(executor).await
    }
}

impl crate::forms::FormSubmission {
    pub async fn delete_many<'e, E>(
        executor: E,
        w: &crate::forms::FormSubmissionWhereInput,
    ) -> sqlx::Result<u64>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut qb = sqlx::QueryBuilder::new(r#"DELETE FROM "form_submissions" WHERE "#);
        if !crate::engine::sqlx_postgres::forms::push_form_submission_where(&mut qb, w) {
            qb.push("TRUE");
        }
        let result = qb.build().execute(executor).await?;
        Ok(result.rows_affected())
    }
}

#[derive(Debug, Clone, Default, PartialEq, Eq, Hash, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct FormSubmissionCountAggregateResult {
    pub id: Option<i64>,
    pub distribution_id: Option<i64>,
    pub template_id: Option<i64>,
    pub template_version_id: Option<i64>,
    pub firm_id: Option<i64>,
    pub data: Option<i64>,
    pub status: Option<i64>,
    pub submitted_at: Option<i64>,
    pub verified_at: Option<i64>,
    pub verified_by_id: Option<i64>,
    #[serde(rename = "_all")]
    pub aggregate_all: Option<i64>,
}

#[derive(Debug, Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct FormSubmissionAvgAggregateResult {}

#[derive(Debug, Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct FormSubmissionSumAggregateResult {}

#[derive(Debug, Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct FormSubmissionMinAggregateResult {
    pub id: Option<String>,
    pub distribution_id: Option<String>,
    pub template_id: Option<String>,
    pub template_version_id: Option<String>,
    pub firm_id: Option<String>,
    pub submitted_at: Option<chrono::DateTime<chrono::Utc>>,
    pub verified_at: Option<chrono::DateTime<chrono::Utc>>,
    pub verified_by_id: Option<String>,
}

#[derive(Debug, Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct FormSubmissionMaxAggregateResult {
    pub id: Option<String>,
    pub distribution_id: Option<String>,
    pub template_id: Option<String>,
    pub template_version_id: Option<String>,
    pub firm_id: Option<String>,
    pub submitted_at: Option<chrono::DateTime<chrono::Utc>>,
    pub verified_at: Option<chrono::DateTime<chrono::Utc>>,
    pub verified_by_id: Option<String>,
}

#[derive(Debug, Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct FormSubmissionAggregateResult {
    #[serde(rename = "_count")]
    pub aggregate_count: Option<FormSubmissionCountAggregateResult>,
    #[serde(rename = "_avg")]
    pub aggregate_avg: Option<FormSubmissionAvgAggregateResult>,
    #[serde(rename = "_sum")]
    pub aggregate_sum: Option<FormSubmissionSumAggregateResult>,
    #[serde(rename = "_min")]
    pub aggregate_min: Option<FormSubmissionMinAggregateResult>,
    #[serde(rename = "_max")]
    pub aggregate_max: Option<FormSubmissionMaxAggregateResult>,
}

impl crate::forms::FormSubmission {
    pub async fn aggregate<'e, E>(
        executor: E,
        input: &crate::forms::FormSubmissionAggregateInput,
    ) -> sqlx::Result<FormSubmissionAggregateResult>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut qb = sqlx::QueryBuilder::new(
            r#"SELECT COUNT(*) AS count_all, COUNT("id") AS count_id, COUNT("distribution_id") AS count_distribution_id, COUNT("template_id") AS count_template_id, COUNT("template_version_id") AS count_template_version_id, COUNT("firm_id") AS count_firm_id, COUNT("data") AS count_data, COUNT("status") AS count_status, COUNT("submitted_at") AS count_submitted_at, COUNT("verified_at") AS count_verified_at, COUNT("verified_by_id") AS count_verified_by_id, MIN("id") AS min_id, MAX("id") AS max_id, MIN("distribution_id") AS min_distribution_id, MAX("distribution_id") AS max_distribution_id, MIN("template_id") AS min_template_id, MAX("template_id") AS max_template_id, MIN("template_version_id") AS min_template_version_id, MAX("template_version_id") AS max_template_version_id, MIN("firm_id") AS min_firm_id, MAX("firm_id") AS max_firm_id, MIN("submitted_at") AS min_submitted_at, MAX("submitted_at") AS max_submitted_at, MIN("verified_at") AS min_verified_at, MAX("verified_at") AS max_verified_at, MIN("verified_by_id") AS min_verified_by_id, MAX("verified_by_id") AS max_verified_by_id FROM "form_submissions" WHERE "#,
        );
        if let Some(w) = &input.r#where {
            if !crate::engine::sqlx_postgres::forms::push_form_submission_where(&mut qb, w) {
                qb.push("TRUE");
            }
        } else {
            qb.push("TRUE");
        }
        let row = qb.build().fetch_one(executor).await?;
        use sqlx::Row;
        Ok(FormSubmissionAggregateResult {
            aggregate_count: input
                .aggregate_count
                .as_ref()
                .map(|req| -> sqlx::Result<_> {
                    Ok(FormSubmissionCountAggregateResult {
                        id: if req.id.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_id")?
                        } else {
                            None
                        },
                        distribution_id: if req.distribution_id.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_distribution_id")?
                        } else {
                            None
                        },
                        template_id: if req.template_id.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_template_id")?
                        } else {
                            None
                        },
                        template_version_id: if req.template_version_id.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_template_version_id")?
                        } else {
                            None
                        },
                        firm_id: if req.firm_id.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_firm_id")?
                        } else {
                            None
                        },
                        data: if req.data.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_data")?
                        } else {
                            None
                        },
                        status: if req.status.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_status")?
                        } else {
                            None
                        },
                        submitted_at: if req.submitted_at.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_submitted_at")?
                        } else {
                            None
                        },
                        verified_at: if req.verified_at.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_verified_at")?
                        } else {
                            None
                        },
                        verified_by_id: if req.verified_by_id.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_verified_by_id")?
                        } else {
                            None
                        },
                        aggregate_all: if req.aggregate_all.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_all")?
                        } else {
                            None
                        },
                    })
                })
                .transpose()?,
            aggregate_avg: input
                .aggregate_avg
                .as_ref()
                .map(|_req| -> sqlx::Result<_> { Ok(FormSubmissionAvgAggregateResult {}) })
                .transpose()?,
            aggregate_sum: input
                .aggregate_sum
                .as_ref()
                .map(|_req| -> sqlx::Result<_> { Ok(FormSubmissionSumAggregateResult {}) })
                .transpose()?,
            aggregate_min: input
                .aggregate_min
                .as_ref()
                .map(|req| -> sqlx::Result<_> {
                    Ok(FormSubmissionMinAggregateResult {
                        id: if req.id.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("min_id")?
                        } else {
                            None
                        },
                        distribution_id: if req.distribution_id.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("min_distribution_id")?
                        } else {
                            None
                        },
                        template_id: if req.template_id.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("min_template_id")?
                        } else {
                            None
                        },
                        template_version_id: if req.template_version_id.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("min_template_version_id")?
                        } else {
                            None
                        },
                        firm_id: if req.firm_id.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("min_firm_id")?
                        } else {
                            None
                        },
                        submitted_at: if req.submitted_at.unwrap_or(false) {
                            row.try_get::<Option<chrono::DateTime<chrono::Utc>>, _>(
                                "min_submitted_at",
                            )?
                        } else {
                            None
                        },
                        verified_at: if req.verified_at.unwrap_or(false) {
                            row.try_get::<Option<chrono::DateTime<chrono::Utc>>, _>(
                                "min_verified_at",
                            )?
                        } else {
                            None
                        },
                        verified_by_id: if req.verified_by_id.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("min_verified_by_id")?
                        } else {
                            None
                        },
                    })
                })
                .transpose()?,
            aggregate_max: input
                .aggregate_max
                .as_ref()
                .map(|req| -> sqlx::Result<_> {
                    Ok(FormSubmissionMaxAggregateResult {
                        id: if req.id.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("max_id")?
                        } else {
                            None
                        },
                        distribution_id: if req.distribution_id.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("max_distribution_id")?
                        } else {
                            None
                        },
                        template_id: if req.template_id.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("max_template_id")?
                        } else {
                            None
                        },
                        template_version_id: if req.template_version_id.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("max_template_version_id")?
                        } else {
                            None
                        },
                        firm_id: if req.firm_id.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("max_firm_id")?
                        } else {
                            None
                        },
                        submitted_at: if req.submitted_at.unwrap_or(false) {
                            row.try_get::<Option<chrono::DateTime<chrono::Utc>>, _>(
                                "max_submitted_at",
                            )?
                        } else {
                            None
                        },
                        verified_at: if req.verified_at.unwrap_or(false) {
                            row.try_get::<Option<chrono::DateTime<chrono::Utc>>, _>(
                                "max_verified_at",
                            )?
                        } else {
                            None
                        },
                        verified_by_id: if req.verified_by_id.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("max_verified_by_id")?
                        } else {
                            None
                        },
                    })
                })
                .transpose()?,
        })
    }
}

impl sqlx::FromRow<'_, sqlx::postgres::PgRow> for crate::forms::SubmissionNote {
    fn from_row(row: &sqlx::postgres::PgRow) -> sqlx::Result<Self> {
        Ok(crate::forms::SubmissionNote {
            id: row.try_get("id")?,
            submission_id: row.try_get("submission_id")?,
            content: row.try_get("content")?,
            created_by_id: row.try_get("created_by_id")?,
            created_at: row.try_get("created_at")?,
            submission: None,
            created_by: None,
        })
    }
}

pub(crate) fn push_submission_note_where(
    qb: &mut sqlx::QueryBuilder<'_, sqlx::Postgres>,
    w: &crate::forms::SubmissionNoteWhereInput,
) -> bool {
    let mut any = false;
    if let Some(f) = &w.id {
        if any {
            qb.push(" AND ");
        }
        any = true;
        crate::engine::sqlx_postgres::filters::push_string_filter(qb, "id", f);
    }
    if let Some(f) = &w.submission_id {
        if any {
            qb.push(" AND ");
        }
        any = true;
        crate::engine::sqlx_postgres::filters::push_string_filter(qb, "submission_id", f);
    }
    if let Some(f) = &w.content {
        if any {
            qb.push(" AND ");
        }
        any = true;
        crate::engine::sqlx_postgres::filters::push_string_filter(qb, "content", f);
    }
    if let Some(f) = &w.created_by_id {
        if any {
            qb.push(" AND ");
        }
        any = true;
        crate::engine::sqlx_postgres::filters::push_string_filter(qb, "created_by_id", f);
    }
    if let Some(f) = &w.created_at {
        if any {
            qb.push(" AND ");
        }
        any = true;
        crate::engine::sqlx_postgres::filters::push_datetime_filter(qb, "created_at", f);
    }
    if let Some(rel) = &w.submission {
        if let Some(sub) = &rel.is {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("EXISTS (SELECT 1 FROM \"form_submissions\" t WHERE t.\"id\" = \"submission_notes\".\"submission_id\" AND (");
            if !crate::engine::sqlx_postgres::forms::push_form_submission_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
        if let Some(sub) = &rel.is_not {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("NOT EXISTS (SELECT 1 FROM \"form_submissions\" t WHERE t.\"id\" = \"submission_notes\".\"submission_id\" AND (");
            if !crate::engine::sqlx_postgres::forms::push_form_submission_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
    }
    if let Some(rel) = &w.created_by {
        if let Some(sub) = &rel.is {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("EXISTS (SELECT 1 FROM \"users\" t WHERE t.\"id\" = \"submission_notes\".\"created_by_id\" AND (");
            if !crate::engine::sqlx_postgres::users::push_user_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
        if let Some(sub) = &rel.is_not {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("NOT EXISTS (SELECT 1 FROM \"users\" t WHERE t.\"id\" = \"submission_notes\".\"created_by_id\" AND (");
            if !crate::engine::sqlx_postgres::users::push_user_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push("))");
        }
    }
    if let Some(group) = &w.and {
        for sub in group {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("(");
            if !push_submission_note_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push(")");
        }
    }
    if let Some(group) = &w.or {
        if any {
            qb.push(" AND ");
        }
        any = true;
        qb.push("(");
        let mut first = true;
        for sub in group {
            if !first {
                qb.push(" OR ");
            }
            first = false;
            qb.push("(");
            if !push_submission_note_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push(")");
        }
        qb.push(")");
    }
    if let Some(group) = &w.not {
        for sub in group {
            if any {
                qb.push(" AND ");
            }
            any = true;
            qb.push("NOT (");
            if !push_submission_note_where(qb, sub) {
                qb.push("TRUE");
            }
            qb.push(")");
        }
    }
    any
}

impl crate::forms::SubmissionNote {
    pub async fn find_unique<'e, E>(
        executor: E,
        w: &crate::forms::SubmissionNoteWhereUniqueInput,
    ) -> sqlx::Result<Option<Self>>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let cols = "\"id\", \"submission_id\", \"content\", \"created_by_id\", \"created_at\"";
        let mut qb =
            sqlx::QueryBuilder::new(format!(r#"SELECT {} FROM "submission_notes" WHERE "#, cols,));
        // For v1, support only the @id field. Composite uniques are out of scope.
        // The WhereUniqueInput has Option<T> per @id/@unique field; we pick the
        // first Some(). Multi-Some inputs cover composite uniques — TODO Task v2.
        if let Some(value) = &w.id {
            qb.push("\"id\" = ");
            qb.push_bind(value.clone());
        } else {
            return Ok(None);
        }
        qb.build_query_as::<Self>().fetch_optional(executor).await
    }
}

impl crate::forms::SubmissionNote {
    pub async fn find_first<'e, E>(
        executor: E,
        w: &crate::forms::SubmissionNoteWhereInput,
    ) -> sqlx::Result<Option<Self>>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut qb = sqlx::QueryBuilder::new(
            r#"SELECT "id", "submission_id", "content", "created_by_id", "created_at" FROM "submission_notes" WHERE "#,
        );
        if !crate::engine::sqlx_postgres::forms::push_submission_note_where(&mut qb, w) {
            qb.push("TRUE");
        }
        qb.push(" LIMIT 1");
        qb.build_query_as::<Self>().fetch_optional(executor).await
    }
}

pub struct SubmissionNoteFindManyBuilder<'a, E> {
    executor: E,
    where_input: &'a crate::forms::SubmissionNoteWhereInput,
    order_by: Vec<crate::forms::SubmissionNoteOrderByWithRelationInput>,
    take: Option<i64>,
    skip: Option<i64>,
}

impl<'a, E> SubmissionNoteFindManyBuilder<'a, E> {
    pub fn order_by(
        mut self,
        order_by: &[crate::forms::SubmissionNoteOrderByWithRelationInput],
    ) -> Self {
        self.order_by = order_by.to_vec();
        self
    }

    pub fn take(mut self, n: i64) -> Self {
        self.take = Some(n);
        self
    }

    pub fn skip(mut self, n: i64) -> Self {
        self.skip = Some(n);
        self
    }

    pub async fn exec<'e>(self) -> sqlx::Result<Vec<crate::forms::SubmissionNote>>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut qb = sqlx::QueryBuilder::new(
            r#"SELECT "id", "submission_id", "content", "created_by_id", "created_at" FROM "submission_notes" WHERE "#,
        );
        if !crate::engine::sqlx_postgres::forms::push_submission_note_where(
            &mut qb,
            self.where_input,
        ) {
            qb.push("TRUE");
        }
        if !self.order_by.is_empty() {
            qb.push(" ORDER BY ");
            let mut first = true;
            for ob in &self.order_by {
                if let Some(o) = ob.id {
                    if !first {
                        qb.push(", ");
                    }
                    first = false;
                    qb.push("\"id\" ");
                    qb.push(crate::engine::sqlx_postgres::filters::sort_order_sql(o));
                }
                if let Some(o) = ob.submission_id {
                    if !first {
                        qb.push(", ");
                    }
                    first = false;
                    qb.push("\"submission_id\" ");
                    qb.push(crate::engine::sqlx_postgres::filters::sort_order_sql(o));
                }
                if let Some(o) = ob.content {
                    if !first {
                        qb.push(", ");
                    }
                    first = false;
                    qb.push("\"content\" ");
                    qb.push(crate::engine::sqlx_postgres::filters::sort_order_sql(o));
                }
                if let Some(o) = ob.created_by_id {
                    if !first {
                        qb.push(", ");
                    }
                    first = false;
                    qb.push("\"created_by_id\" ");
                    qb.push(crate::engine::sqlx_postgres::filters::sort_order_sql(o));
                }
                if let Some(o) = ob.created_at {
                    if !first {
                        qb.push(", ");
                    }
                    first = false;
                    qb.push("\"created_at\" ");
                    qb.push(crate::engine::sqlx_postgres::filters::sort_order_sql(o));
                }
                // TODO(prisma-rust-generator): sorting by relation `submission` not yet supported.
                let _ = &ob.submission;
                // TODO(prisma-rust-generator): sorting by relation `createdBy` not yet supported.
                let _ = &ob.created_by;
            }
        }
        if let Some(n) = self.take {
            qb.push(" LIMIT ");
            qb.push_bind(n);
        }
        if let Some(n) = self.skip {
            qb.push(" OFFSET ");
            qb.push_bind(n);
        }
        qb.build_query_as::<crate::forms::SubmissionNote>()
            .fetch_all(self.executor)
            .await
    }
}

impl crate::forms::SubmissionNote {
    pub fn find_many<'a, E>(
        executor: E,
        w: &'a crate::forms::SubmissionNoteWhereInput,
    ) -> SubmissionNoteFindManyBuilder<'a, E> {
        SubmissionNoteFindManyBuilder {
            executor,
            where_input: w,
            order_by: Vec::new(),
            take: None,
            skip: None,
        }
    }
}

impl crate::forms::SubmissionNote {
    pub async fn count<'e, E>(
        executor: E,
        w: Option<&crate::forms::SubmissionNoteWhereInput>,
    ) -> sqlx::Result<i64>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut qb = sqlx::QueryBuilder::new(r#"SELECT COUNT(*) FROM "submission_notes" WHERE "#);
        if let Some(w) = w {
            if !crate::engine::sqlx_postgres::forms::push_submission_note_where(&mut qb, w) {
                qb.push("TRUE");
            }
        } else {
            qb.push("TRUE");
        }
        let row = qb.build_query_as::<(i64,)>().fetch_one(executor).await?;
        Ok(row.0)
    }
}

impl crate::forms::SubmissionNote {
    pub async fn create<'e, E>(
        executor: E,
        input: crate::forms::SubmissionNoteUncheckedCreateInput,
    ) -> sqlx::Result<Self>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut cols = String::new();
        cols.push_str("\"submission_id\"");
        cols.push_str(", ");
        cols.push_str("\"content\"");
        cols.push_str(", ");
        cols.push_str("\"created_by_id\"");
        let mut qb = sqlx::QueryBuilder::new(format!(
            r#"INSERT INTO "submission_notes" ({}) VALUES ("#,
            cols,
        ));
        qb.push_bind(input.submission_id);
        qb.push(", ");
        qb.push_bind(input.content);
        qb.push(", ");
        qb.push_bind(input.created_by_id);
        qb.push(r#") RETURNING "id", "submission_id", "content", "created_by_id", "created_at""#);
        qb.build_query_as::<Self>().fetch_one(executor).await
    }
}

impl crate::forms::SubmissionNote {
    pub async fn create_many<'e, E>(
        executor: E,
        inputs: &[crate::forms::SubmissionNoteUncheckedCreateInput],
    ) -> sqlx::Result<u64>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        if inputs.is_empty() {
            return Ok(0);
        }
        let mut qb = sqlx::QueryBuilder::new(
            r#"INSERT INTO "submission_notes" ("submission_id", "content", "created_by_id") "#,
        );
        qb.push_values(inputs, |mut row, input| {
            row.push_bind(input.submission_id.clone());
            row.push_bind(input.content.clone());
            row.push_bind(input.created_by_id.clone());
        });
        let result = qb.build().execute(executor).await?;
        Ok(result.rows_affected())
    }
}

impl crate::forms::SubmissionNote {
    pub async fn update<'e, E>(
        executor: E,
        w: &crate::forms::SubmissionNoteWhereUniqueInput,
        input: crate::forms::SubmissionNoteUncheckedUpdateInput,
    ) -> sqlx::Result<Self>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut qb = sqlx::QueryBuilder::new(r#"UPDATE "submission_notes" SET "#);
        let mut first = true;
        if let Some(op) = input.id {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"id\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.submission_id {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"submission_id\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.content {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"content\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.created_by_id {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"created_by_id\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.created_at {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"created_at\" = ");
                qb.push_bind(v);
            }
        }
        if first {
            // Nothing to update — fall back to a plain find_unique so the
            // caller still gets the row back (or RowNotFound if absent).
            return Self::find_unique(executor, w)
                .await?
                .ok_or(sqlx::Error::RowNotFound);
        }
        qb.push(" WHERE ");
        if let Some(value) = &w.id {
            qb.push("\"id\" = ");
            qb.push_bind(value.clone());
        } else {
            return Err(sqlx::Error::RowNotFound);
        }
        qb.push(r#" RETURNING "id", "submission_id", "content", "created_by_id", "created_at""#);
        qb.build_query_as::<Self>().fetch_one(executor).await
    }
}

impl crate::forms::SubmissionNote {
    pub async fn update_many<'e, E>(
        executor: E,
        w: &crate::forms::SubmissionNoteWhereInput,
        input: crate::forms::SubmissionNoteUncheckedUpdateManyInput,
    ) -> sqlx::Result<u64>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut qb = sqlx::QueryBuilder::new(r#"UPDATE "submission_notes" SET "#);
        let mut first = true;
        if let Some(op) = input.id {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"id\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.submission_id {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"submission_id\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.content {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"content\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.created_by_id {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"created_by_id\" = ");
                qb.push_bind(v);
            }
        }
        if let Some(op) = input.created_at {
            if let Some(v) = op.set {
                if !first {
                    qb.push(", ");
                }
                first = false;
                qb.push("\"created_at\" = ");
                qb.push_bind(v);
            }
        }
        if first {
            // No-op update — return 0 rows affected without issuing SQL.
            return Ok(0);
        }
        qb.push(" WHERE ");
        if !crate::engine::sqlx_postgres::forms::push_submission_note_where(&mut qb, w) {
            qb.push("TRUE");
        }
        let result = qb.build().execute(executor).await?;
        Ok(result.rows_affected())
    }
}

impl crate::forms::SubmissionNote {
    pub async fn delete<'e, E>(
        executor: E,
        w: &crate::forms::SubmissionNoteWhereUniqueInput,
    ) -> sqlx::Result<Self>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut qb = sqlx::QueryBuilder::new(r#"DELETE FROM "submission_notes" WHERE "#);
        if let Some(value) = &w.id {
            qb.push("\"id\" = ");
            qb.push_bind(value.clone());
        } else {
            return Err(sqlx::Error::RowNotFound);
        }
        qb.push(r#" RETURNING "id", "submission_id", "content", "created_by_id", "created_at""#);
        qb.build_query_as::<Self>().fetch_one(executor).await
    }
}

impl crate::forms::SubmissionNote {
    pub async fn delete_many<'e, E>(
        executor: E,
        w: &crate::forms::SubmissionNoteWhereInput,
    ) -> sqlx::Result<u64>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut qb = sqlx::QueryBuilder::new(r#"DELETE FROM "submission_notes" WHERE "#);
        if !crate::engine::sqlx_postgres::forms::push_submission_note_where(&mut qb, w) {
            qb.push("TRUE");
        }
        let result = qb.build().execute(executor).await?;
        Ok(result.rows_affected())
    }
}

#[derive(Debug, Clone, Default, PartialEq, Eq, Hash, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct SubmissionNoteCountAggregateResult {
    pub id: Option<i64>,
    pub submission_id: Option<i64>,
    pub content: Option<i64>,
    pub created_by_id: Option<i64>,
    pub created_at: Option<i64>,
    #[serde(rename = "_all")]
    pub aggregate_all: Option<i64>,
}

#[derive(Debug, Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct SubmissionNoteAvgAggregateResult {}

#[derive(Debug, Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct SubmissionNoteSumAggregateResult {}

#[derive(Debug, Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct SubmissionNoteMinAggregateResult {
    pub id: Option<String>,
    pub submission_id: Option<String>,
    pub content: Option<String>,
    pub created_by_id: Option<String>,
    pub created_at: Option<chrono::DateTime<chrono::Utc>>,
}

#[derive(Debug, Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct SubmissionNoteMaxAggregateResult {
    pub id: Option<String>,
    pub submission_id: Option<String>,
    pub content: Option<String>,
    pub created_by_id: Option<String>,
    pub created_at: Option<chrono::DateTime<chrono::Utc>>,
}

#[derive(Debug, Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct SubmissionNoteAggregateResult {
    #[serde(rename = "_count")]
    pub aggregate_count: Option<SubmissionNoteCountAggregateResult>,
    #[serde(rename = "_avg")]
    pub aggregate_avg: Option<SubmissionNoteAvgAggregateResult>,
    #[serde(rename = "_sum")]
    pub aggregate_sum: Option<SubmissionNoteSumAggregateResult>,
    #[serde(rename = "_min")]
    pub aggregate_min: Option<SubmissionNoteMinAggregateResult>,
    #[serde(rename = "_max")]
    pub aggregate_max: Option<SubmissionNoteMaxAggregateResult>,
}

impl crate::forms::SubmissionNote {
    pub async fn aggregate<'e, E>(
        executor: E,
        input: &crate::forms::SubmissionNoteAggregateInput,
    ) -> sqlx::Result<SubmissionNoteAggregateResult>
    where
        E: sqlx::Executor<'e, Database = sqlx::Postgres>,
    {
        let mut qb = sqlx::QueryBuilder::new(
            r#"SELECT COUNT(*) AS count_all, COUNT("id") AS count_id, COUNT("submission_id") AS count_submission_id, COUNT("content") AS count_content, COUNT("created_by_id") AS count_created_by_id, COUNT("created_at") AS count_created_at, MIN("id") AS min_id, MAX("id") AS max_id, MIN("submission_id") AS min_submission_id, MAX("submission_id") AS max_submission_id, MIN("content") AS min_content, MAX("content") AS max_content, MIN("created_by_id") AS min_created_by_id, MAX("created_by_id") AS max_created_by_id, MIN("created_at") AS min_created_at, MAX("created_at") AS max_created_at FROM "submission_notes" WHERE "#,
        );
        if let Some(w) = &input.r#where {
            if !crate::engine::sqlx_postgres::forms::push_submission_note_where(&mut qb, w) {
                qb.push("TRUE");
            }
        } else {
            qb.push("TRUE");
        }
        let row = qb.build().fetch_one(executor).await?;
        use sqlx::Row;
        Ok(SubmissionNoteAggregateResult {
            aggregate_count: input
                .aggregate_count
                .as_ref()
                .map(|req| -> sqlx::Result<_> {
                    Ok(SubmissionNoteCountAggregateResult {
                        id: if req.id.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_id")?
                        } else {
                            None
                        },
                        submission_id: if req.submission_id.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_submission_id")?
                        } else {
                            None
                        },
                        content: if req.content.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_content")?
                        } else {
                            None
                        },
                        created_by_id: if req.created_by_id.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_created_by_id")?
                        } else {
                            None
                        },
                        created_at: if req.created_at.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_created_at")?
                        } else {
                            None
                        },
                        aggregate_all: if req.aggregate_all.unwrap_or(false) {
                            row.try_get::<Option<i64>, _>("count_all")?
                        } else {
                            None
                        },
                    })
                })
                .transpose()?,
            aggregate_avg: input
                .aggregate_avg
                .as_ref()
                .map(|_req| -> sqlx::Result<_> { Ok(SubmissionNoteAvgAggregateResult {}) })
                .transpose()?,
            aggregate_sum: input
                .aggregate_sum
                .as_ref()
                .map(|_req| -> sqlx::Result<_> { Ok(SubmissionNoteSumAggregateResult {}) })
                .transpose()?,
            aggregate_min: input
                .aggregate_min
                .as_ref()
                .map(|req| -> sqlx::Result<_> {
                    Ok(SubmissionNoteMinAggregateResult {
                        id: if req.id.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("min_id")?
                        } else {
                            None
                        },
                        submission_id: if req.submission_id.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("min_submission_id")?
                        } else {
                            None
                        },
                        content: if req.content.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("min_content")?
                        } else {
                            None
                        },
                        created_by_id: if req.created_by_id.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("min_created_by_id")?
                        } else {
                            None
                        },
                        created_at: if req.created_at.unwrap_or(false) {
                            row.try_get::<Option<chrono::DateTime<chrono::Utc>>, _>(
                                "min_created_at",
                            )?
                        } else {
                            None
                        },
                    })
                })
                .transpose()?,
            aggregate_max: input
                .aggregate_max
                .as_ref()
                .map(|req| -> sqlx::Result<_> {
                    Ok(SubmissionNoteMaxAggregateResult {
                        id: if req.id.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("max_id")?
                        } else {
                            None
                        },
                        submission_id: if req.submission_id.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("max_submission_id")?
                        } else {
                            None
                        },
                        content: if req.content.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("max_content")?
                        } else {
                            None
                        },
                        created_by_id: if req.created_by_id.unwrap_or(false) {
                            row.try_get::<Option<String>, _>("max_created_by_id")?
                        } else {
                            None
                        },
                        created_at: if req.created_at.unwrap_or(false) {
                            row.try_get::<Option<chrono::DateTime<chrono::Utc>>, _>(
                                "max_created_at",
                            )?
                        } else {
                            None
                        },
                    })
                })
                .transpose()?,
        })
    }
}
